# Task 010 - 2026-05-17

## 任务
- `item-detail.html` 手机版：完成 `image-10` 对应区域改版，按 Figma `node-id=722:21922` 调整内容与外层卡片间距。

## 修改内容
- 仅针对手机版进行修改，不影响 PC。
- 使用 Figma MCP 读取设计稿后，调整 `Rules` 模块目标卡片布局：
  - 卡片内容内边距按设计稿调整为 `10px`（`1.25rem`）。
  - 卡片内部元素垂直间距保持 `2px`（`0.25rem`）。
- 为避免缓存导致样式未更新，升级 `item-detail.css` 版本参数并重新构建页面。
- 同步更新 `.less` 与 `.css`，并按构建规则生成根目录产物。

## 变更文件
- `css/detail/item-detail.less`
- `css/detail/item-detail.css`
- `src/pages/item-detail.html`
- `item-detail.html`
- `codex_tasks/TASKS.md`
