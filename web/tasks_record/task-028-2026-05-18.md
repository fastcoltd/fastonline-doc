# Task 028 - 2026-05-18

## 任务信息
- 任务：`demand-detail.html` 手机版提交投标表单（Upload 区）按 Figma 调整，并修复上传后图片显示异常
- 目标：对齐 Figma 节点 `1019:39493`（未选图）与 `1158:46145`（选图后），仅改移动端，不影响 PC；上传后缩略图固定为设计尺寸，并支持点击查看大图。

## 修改文件
- `src/pages/demand-detail.html`
- `css/detail/demand-detail.less`
- `css/detail/demand-detail.css`
- `js/detail/demand-detail.js`
- `demand-detail.html`
- `codex_tasks/TASKS.md`

## Diff 摘要
1. 表单与上传区移动端布局对齐
- 调整 `bid-form` 在移动端的字段顺序、间距、输入框尺寸与按钮样式，匹配 Figma 提交投标区结构。
- Upload 区标签、按钮、提示文案按设计重排与对齐。

2. 上传缩略图显示修复（重点）
- 上传预览改为固定缩略图样式，不再使用可能受根字号影响的尺寸：
  - 缩略图容器固定 `60px x 60px`
  - 增加 `min/max` 与 `flex/-webkit-flex` 约束
  - 删除按钮固定 `15px`
- 对关键尺寸增加 `!important` 兜底，避免机型内核或其他样式覆盖导致异常放大。

3. 交互增强：点击缩略图查看大图
- 新增上传图片预览遮罩层与关闭按钮。
- 支持点击缩略图打开大图、点击关闭按钮/遮罩空白处/按 `Esc` 关闭。

4. 兼容与缓存处理
- 为当前页引入的资源增加版本参数，确保移动端拿到最新样式与脚本：
  - `css/detail/demand-detail.css?v=20260518b`
  - `js/detail/demand-detail.js?v=20260518b`

5. 页面产物同步
- 按构建规则执行 `node scripts/build-pages.js`，同步生成根目录 `demand-detail.html`。

6. 任务状态更新
- 将 `codex_tasks/TASKS.md` 对应任务状态由“未完成”更新为“已完成”。

## 校验
- 已执行 `node --check js/detail/demand-detail.js`。
- 已执行 `node scripts/build-pages.js`。
- 已使用 Chrome MCP 实测上传流程：
  - 缩略图实际尺寸 `60x60`
  - 计算样式 `width/height/min/max/flex-basis = 60px`
  - 点击缩略图可打开大图并正常关闭

## 结果
- `demand-detail.html` 手机版 Upload 区已完成样式与交互修复，上传图片不再超出页面显示区域。
- 任务状态、任务记录与页面产物均已同步。
