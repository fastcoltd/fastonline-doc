# Task 089：post-detail.html 顶部 Blog 区域跳转

## 任务内容

post-detail.html 页面手机版和 PC 版中，顶部如 image-126.png 所示的 `Blog：头像 Erinasa` 区域，点击后需要跳转到 blog.html 页面。

## 修改页面

- post-detail.html

## 修改内容

- 将 `src/pages/post-detail.html` 顶部 Blog 信息区域从普通容器改为链接，设置 `href="blog.html"`。
- 在 `css/detail/post-detail.less` 和 `css/detail/post-detail.css` 中为 `.info-item` 补充链接样式重置，保持原有视觉样式不出现默认下划线或颜色变化。
- 执行页面构建，更新根目录静态 HTML 的防缓存版本。

## 验证

- 执行 `node scripts/build-pages.js`，构建成功，防缓存版本更新为 `20260609161937`。
- 确认根目录 `post-detail.html` 中顶部 Blog 区域已生成 `href="blog.html"` 链接。
