# Task 055 - 2026-05-22

## 任务
`store-detail.html` 手机版在指定区域上方增加标题 `Items`（参考 Figma `node-id=1149:40930`）。

## 修改页面
- `src/pages/store-detail.html`
- `css/detail/store-detail.less`
- `css/detail/store-detail.css`
- `codex_tasks/TASKS.md`
- 根目录 `store-detail.html` 产物（`node scripts/build-pages.js` 生成，含缓存版本更新）

## 变更摘要
- 仅针对手机版修改，不影响 PC。
- 在 `#Items` 区块顶部新增标题：`<span class="item-detail-right-title item-detail-mobile-only-title">Items</span>`。
- 新增 `item-detail-mobile-only-title` 复用类：默认隐藏，`max-width: 768px` 下显示，避免影响桌面端。
- 修复移动端样式覆盖顺序问题，确保一加 Ace5 Pro 等机型可稳定显示该标题。
- 执行 `node scripts/build-pages.js`，同步生成根目录 `store-detail.html`，刷新防缓存版本号。
- 将 `TASKS.md` 对应任务状态更新为“已完成”。

## 影响范围
- 仅影响 `store-detail.html` 手机版 `Items` 区块标题展示。
- 不影响其他页面与 PC 布局逻辑。
