# Task 067 - 2026-05-30

## 任务
`search-all.html` 手机版中，Items tab 与 Demands tab 的 `BUY NOW` 按钮样式不一致；需要按 Items tab 按钮样式统一，并同步到项目内移动端相关 `BUY NOW` 按钮。

## 修改页面
- `css/search-all.less`
- `css/search-all.css`
- `css/demand.less`
- `css/demand.css`
- `css/tag-all.less`
- `css/tag-all.css`
- `css/attribute-all.less`
- `css/attribute-all.css`
- `css/common.less`
- `css/common.css`
- `codex_tasks/TASKS.md`

## 变更摘要
- 仅调整移动端按钮样式，未改动 PC 样式。
- 统一 `search-all` 列表卡片内 `.buy-btn` 为 Items tab 按钮规格（高度、圆角、字体、边框、阴影）。
- 统一 `demand` 横向移动端卡片按钮 `.demand-all-horizontal-mobile-button` 到同一规格，并补齐 `disabled/:disabled/[aria-disabled]` 状态。
- 统一 `tag-all` 与 `attribute-all` 横向移动端卡片按钮 `.tag-mobile-horizontal-item-buy-btn` 到同一规格，并补齐禁用态。
- 为移动端通用 `.item-button-box` 增加白底与阴影，保证项目内相关 `BUY NOW` 按钮视觉一致。
- 用户确认“任务完成”后，将 `codex_tasks/TASKS.md` 对应任务状态更新为“已完成”。

## 影响范围
- 影响移动端 `BUY NOW` 按钮样式展示（Items / Demands / 标签页相关卡片）。
- 不影响 PC 页面及业务逻辑。
