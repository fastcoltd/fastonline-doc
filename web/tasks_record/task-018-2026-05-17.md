# Task 018 - 2026-05-17

## 任务信息
- 任务：`post-detail.html` 手机版（Related items 模块）
- 目标：每一页只展示 1 行，每行展示 2 个 item

## 修改文件
- `js/detail/post-detail.js`
- `codex_tasks/TASKS.md`

## Diff 摘要
1. 新增移动端重排逻辑（仅手机版）
- 在 `post-detail.js` 新增 `optimizeRelatedItemsForMobile()`。
- 仅在 `max-width: 768px` 或触屏设备下执行，不影响 PC 端。
- 将 `#best-items` 轮播中的卡片重新分组为每页 2 个，确保每页仅 1 行。
- 同步重建分页指示点数量，并在轮播初始化前执行。

2. 任务状态更新
- 将 `codex_tasks/TASKS.md` 对应任务状态由“未完成”更新为“已完成”。

## 结果
- `post-detail.html` 手机版 Related items 已按要求调整为“每页 1 行、每行 2 个”。
- PC 端布局与交互保持不变。
