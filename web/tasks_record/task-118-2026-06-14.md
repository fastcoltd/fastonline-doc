# Task 118：item-detail.html 页面手机版增加统计悬浮按钮

## 任务内容

item-detail.html 页面手机版需要增加一个悬浮按钮，悬浮按钮样式与 post-detail.html 手机版悬浮按钮一致。点击后展示 Figma 节点中的 Item's Statistics 页面内容，也就是 PC 端右侧统计卡片里的内容。

## 修改页面

- `item-detail.html`

## Diff 总结

- 在 `src/pages/item-detail.html` 中复用 `src/partials/components/detail-page-menu.html` 悬浮按钮组件，并为统计面板增加 `id="item-detail-statistics-panel"` 和 `aria-hidden`。
- 在 `css/detail/item-detail.less` 中补充 `item-detail` 手机版统计抽屉样式，并同步更新 `css/detail/item-detail.css`：
  - 悬浮按钮移动端样式与 `post-detail.html` 一致。
  - 统计面板移动端全屏展示，顶部显示标题和关闭按钮。
  - 统计面板内容区域支持内部滚动。
  - `Overview` 和 `Sales Count` 图表高度调整为 `38rem`，避免手机版内容挤在一起。
- 在 `js/detail/item-detail.js` 中调整统计面板打开/关闭逻辑：
  - 使用 `is-open` 控制移动端显示状态。
  - 同步维护 `aria-expanded` 和 `aria-hidden`。
  - 避免每次点击悬浮按钮重复绑定关闭事件。
- 执行 `node scripts/build-pages.js` 重新生成根目录静态页面，并刷新 CSS/JS 防缓存版本。

## 验证

- `node --check js/detail/item-detail.js` 通过。
- `node --check scripts/build-pages.js` 通过。
- `node scripts/build-pages.js` 通过，缓存版本刷新为 `20260613193003`。

