# Task 070 - 2026-06-01

## 任务
`index.html` 手机版中，不同模块 item 的标签高度与样式不一致；按 Figma 节点 `263:1968` 统一封装并应用到首页各类 item 标签组件，确保手机端展示一致。

## 修改页面
- `css/index.less`
- `css/index.css`
- `src/pages/index.html`
- `index.html`
- `codex_tasks/TASKS.md`

## 变更摘要
- 仅调整首页相关的**手机版**标签样式（`@media (max-width: 768px)`），不改 PC 端布局与逻辑。
- 读取 Figma 节点 `263:1968` 后，抽取并复用首页移动端标签样式：
  - 标签容器：`height: 2.25rem`、`padding: 0 0.625rem`、`border-radius: 2.5rem`
  - 边框与阴影：`0.1rem solid #d9d9d9` + `0 0.2rem 0 rgba(0,0,0,0.02)`
  - 文本：`font-size: 1.25rem`、`line-height: 1.5rem`、`color: #6d6d6d`
- 将统一样式应用到首页多个模块标签：`Best stores`、`Hot campaigns`、`Popular demands（mobile）`、`Host posts`、`Comments`。
- 处理防缓存：
  - `src/pages/index.html` 更新 `css/index.css` 版本参数。
  - 执行 `node scripts/build-pages.js` 生成根目录 `index.html`，资源版本号同步更新，确保手机端（含一加 Ace5 Pro）可拉取最新样式。
- 用户确认“任务完成”后，将 `codex_tasks/TASKS.md` 对应任务状态由“未完成”改为“已完成”。

## 影响范围
- 仅影响首页 `index.html` 的手机版标签组件视觉统一性。
- 不影响其他页面业务逻辑，不影响 PC 端展示。
