# Task 076：index.html 页面手机版 Best Items 收藏状态修复

## 任务内容

index.html 页面的手机版需要修改。Best items 中的 item，需要修改，其中的收藏和取消收藏按钮，需要使用项目中现有的收藏和取消收藏组件。如果已经使用了，请检查为什么收藏状态颜色变成了白色，需要收藏状态是红色。

## 修改页面

- `index.html`

## Diff 总结

- 确认 Best Items item 已通过 `src/partials/components/item-card-vertical.html` 复用项目现有 `item-like-button` 收藏组件。
- 修复首页移动端 Best Items 选中收藏按钮变白的问题：在 `css/index.less` 与 `css/index.css` 中为 `#best-items` 的选中态收藏按钮增加局部覆盖，取消移动端通用白色滤镜，让 `Vector_sel.png` 红色选中态正常显示。
- 运行 `node scripts/build-pages.js` 重新生成根目录 `index.html`，防缓存版本更新为 `20260607083604`。

## 校验

- `node scripts/build-pages.js` 执行成功。
- `css/index.less`、`css/index.css` 基础括号平衡检查通过。
- 已发送飞书通知。
