# Task 047 - 2026-05-20

## 任务
`fastesp-service.html` 手机版顶部布局修复：完全复用 `item-all.html` 顶部组件结构，保证展示一致。

## 修改页面
- `src/pages/fastesp-service.html`
- `fastesp-service.html`（由 `node scripts/build-pages.js` 生成）
- `codex_tasks/TASKS.md`

## 变更摘要
- 仅做最小化改动，聚焦手机版顶部区域，不影响 PC 端其它功能。
- 将 `fastesp-service` 页面顶部容器类从
  - `page-head page-top-sticky page-top-sticky-main`（含 `id="stickyHeader"`）
  调整为
  - `page-head page-head-main`
  与 `item-all.html` 顶部结构保持一致。
- 按源码构建规则执行 `node scripts/build-pages.js`，同步生成根目录 `fastesp-service.html`，并刷新防缓存版本号（`20260520091319`）。
- 已将 `TASKS.md` 中该任务状态更新为“已完成”。

## 影响范围
- 仅影响 `fastesp-service.html` 页面顶部头部容器结构表现。
- 其余页面与 PC 端逻辑不变。
