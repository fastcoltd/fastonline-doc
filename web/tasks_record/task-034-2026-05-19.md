# Task 034 - 2026-05-19

## 任务
`become-seller.html` 手机版中 `image-41` 对应区域布局不正确，要求参考 `item-all.html` 同位置样式，顶部功能栏、搜索区与面包屑区域保持一致。

## 修改页面
- `src/pages/become-seller.html`
- `css/become-seller.less`
- `css/become-seller.css`
- `become-seller.html`（构建产物）

## 变更摘要
- 将页面顶部容器由 `page-top-sticky page-top-sticky-main` 调整为与 `item-all.html` 一致的 `page-head page-head-main`，复用同一套移动端头部与搜索样式规则。
- 在页面顶部新增并复用面包屑组件：`page-home-breadcrumb`，文案为 `Become a seller`，并通过 `seller-apply-mobile-bread` 仅在移动端显示。
- 补齐与参考页一致的移动端头部配置（`viewport-fit=cover`、`mobile-web-app-capable`、`apple-mobile-web-app-capable`、`apple-mobile-web-app-status-bar-style`）。
- 保持 PC 端不变，仅调整手机版展示。

## 验证
- 已执行页面构建：`node scripts/build-pages.js`。
- 已按要求调用 chrome mcp 对比 `become-seller.html` 与 `item-all.html` 移动端首屏，确认顶部功能栏与搜索区对齐。

## 影响范围
- 仅影响 `become-seller.html` 手机版顶部区域（功能栏、搜索区、面包屑）的布局与显示。
- 不影响其他页面与业务交互逻辑。
