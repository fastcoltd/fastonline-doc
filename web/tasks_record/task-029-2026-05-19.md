# Task 029 - 2026-05-19

## 任务信息
- 任务：`index.html` 右上角 `Join` 系列弹框（手机版）底部组件增加底部外边距
- 目标：仅修改移动端，不影响 PC；确保登录/注册/2FA/重置流程相关弹框底部不贴屏

## 修改文件
- `css/signin.less`
- `css/signin.css`
- `src/pages/index.html`
- `index.html`
- `codex_tasks/TASKS.md`

## Diff 摘要
1. 移动端底部间距规则补齐
- 在 `@media (max-width: 768px)` 中为以下弹框底部组件设置 `margin-bottom: 40px`：
  - `#signin-login .signin-otherway-box`
  - `#signin-regist .signin-otherway-box`
  - `#signin-2fa .signin-otherway-box`
  - `#signin-reset-2fa .signin-otherway-box`
  - `#signin-resetpwd .login-apple-box`

2. 增加容器级兜底留白（避免机型差异导致视觉贴底）
- 为 `#signin-login/#signin-regist/#signin-2fa/#signin-reset-2fa/#signin-resetpwd` 增加：
  - `padding-bottom: max(40px, env(safe-area-inset-bottom, 0px))`
  - `box-sizing: border-box`

3. 处理移动端缓存导致“样式未更新”
- 将 `index.html` 引入的 `signin.css` 增加版本参数：
  - `css/signin.css?v=20260519-join-40`
- 同步修改源码页 `src/pages/index.html`，并执行构建同步根目录页面产物。

4. 任务状态更新
- `codex_tasks/TASKS.md` 对应任务状态由“未完成”改为“已完成”。

## 校验
- 已执行 `node scripts/build-pages.js` 同步页面产物。
- 已通过 Chrome MCP 在移动端视口复测：
  - `window.innerWidth = 390`，`(max-width:768px) = true`
  - `signin.css` 实际加载为带版本号 URL
  - 目标节点计算样式 `margin-bottom = 40px`、`padding-bottom = 40px`

## 结果
- `index.html` 的 Join 系列弹框（手机版）底部留白已生效，且已加入安全区兜底与缓存刷新处理。
