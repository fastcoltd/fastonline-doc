# Task 036 - 2026-05-19

## 任务
`index.html` 手机版品牌区块（Brand）需要去掉外部卡片效果，参考 Figma `node-id=263:995` 1:1 还原。

## 修改页面
- `css/index.less`
- `css/index.css`
- `codex_tasks/TASKS.md`

## 变更摘要
- 仅在移动端媒体查询（`max-width: 768px`）中调整 `Brand` 外层容器 `.brand` 样式：
  - 去掉圆角：`border-radius: 0;`
  - 去掉阴影：`box-shadow: none;`
  - 取消外层裁剪：`overflow: visible;`
- 保持结构不变，仅去除“外部卡片”视觉，不影响 PC 端样式与其他模块。
- 同步更新 `less` 源码与 `css` 编译产物，保持一致。

## 影响范围
- 仅影响 `index.html` 手机版 Brand 区块外层容器视觉表现。
- 不影响 PC 端与其他页面功能。
