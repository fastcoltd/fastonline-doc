# Task 033 - 2026-05-19

## 任务
`become-seller.html` 手机版中“提交申请”按钮背景颜色不正确，需按参考图改为与 `Accept All` 按钮一致的主色。

## 修改页面
- `css/become-seller.less`
- `css/become-seller.css`

## 变更摘要
- 仅在移动端媒体查询（`max-width: 768px`）中覆盖提交按钮颜色：
  - `background-color: var(--primary-color);`
  - `border-color: var(--primary-color);`
- 复用全局主色变量 `--primary-color`，与参考样式保持一致。
- 保持 PC 端样式不变；仅影响手机版 `become-seller.html` 提交按钮视觉。

## 影响范围
- 仅影响 `become-seller.html` 手机版提交申请按钮颜色。
- 不影响按钮交互逻辑与其他页面功能。
