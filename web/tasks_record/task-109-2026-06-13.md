# Task 109：search-all.html页面的手机版和 pc 版需要修改

## 任务内容

search-all.html show more 点了以后，增加一页数据。举个例子：选中 all tab，原本展示的数据是每种类型都有一条，则show more 点击以后，还是这些数据，增加一页。选择其他 tab 也是一样，比如选中 items，原本只展示 1 条数据，那么点击 show more 以后，则增加一条数据，其他 tab 以此类推

## 修改页面

- search-all.html

## Diff 总结

- `js/search-all.js`
  - 将原有模拟加载逻辑改为基于首屏搜索卡片模板追加结果。
  - `All` tab 点击 `Show More` 时追加 items、campaigns、posts、stores、demands 各一条。
  - 单独 tab 点击 `Show More` 时追加当前类型一条。
  - 区分 PC 和手机版模板，保持两端展示结构一致。
  - 修复 PC tab 过滤只显示每类第一张卡片的问题，追加后的同类卡片也会显示。
- `search-all.html`
  - 通过 `node scripts/build-pages.js` 生成根目录产物，并刷新防缓存版本参数。

## 检查

- `node --check js/search-all.js`
- `node scripts/build-pages.js`
- `git diff --check`

