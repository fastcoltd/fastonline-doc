# Task 086：store-detail.html Helpful No 按钮方向修正

## 任务内容

store-detail.html 页面手机版和 PC 版中，Reviews 的 `Helpful?` 区域里 `No` 文本前的按钮方向反了。需要让 No 按钮展示为拇指朝下，选中状态样式保持不变。

## 修改页面

- store-detail.html

## 修改内容

- 在 `css/detail/store-detail.less` 中补齐 `.item-detail-review-tool-icon-no` 的旋转规则，让未选中状态的 No 图标方向与选中状态一致，均为拇指朝下。
- 同步更新 `css/detail/store-detail.css`，保持 LESS 源码与 CSS 产物一致。
- 执行页面构建，更新根目录静态 HTML 的防缓存版本。

## 验证

- 执行 `node scripts/build-pages.js`，构建成功。
- 确认 `store-detail.html` 已引用最新 `css/detail/store-detail.css?v=20260609072028`。
- 确认 No 图标对应的 `.item-detail-review-tool-icon-no` 已具备 `rotate(180deg)` 方向规则。
