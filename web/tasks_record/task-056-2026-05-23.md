# Task 056 - 2026-05-23

## 任务
`store-detail.html` 手机版指定筛选区域（Figma `node-id=1004:37751`）按设计稿 1:1 还原，重点修正输入框高度与 placeholder 样式。

## 修改页面
- `src/pages/store-detail.html`
- `css/detail/store-detail.less`
- `css/detail/store-detail.css`
- 根目录 `store-detail.html` 产物（`node scripts/build-pages.js` 生成）
- `codex_tasks/TASKS.md`

## 变更摘要
- 仅修改手机版样式，不影响 PC 端。
- 对齐筛选区容器样式：内边距、圆角、行间距、模块间距。
- 对齐 `Brand/Tag` 输入框样式：高度 `30px`、圆角 `10px`、字号/颜色、placeholder 样式。
- 对齐 `Search by title...` 输入框与图标区域样式：高度、字号、占位符、图标区宽高与内边距。
- 修正移动端 focus 态，移除蓝色高亮阴影，避免偏离设计稿。
- 增加安卓兼容样式：`appearance: none`、`-webkit-appearance: none`、`-webkit-text-size-adjust: 100%`，兼容一加 Ace5 Pro。
- 补充防缓存：`src/pages/store-detail.html` 中 `store-detail.css` 增加版本参数；并执行 `node scripts/build-pages.js` 生成根目录页面产物，确保手机端拿到最新样式。
- 将 `TASKS.md` 对应任务状态更新为“已完成”。

## 影响范围
- 仅影响 `store-detail.html` 手机版 `Items` 区块筛选区域（Brand/Tag/Search）。
- 不影响其他页面及 PC 布局。
