# Task 002 完成记录

- 任务：`codex_tasks/TASKS.MD` - Task 002（`attribute-all.html` 与 `tag-all.html` 排序浮层遮挡修复）
- 完成日期：2026-05-16
- 修改范围：仅手机版样式层级修复，不影响 PC 端

## 问题确认

排序浮动菜单已渲染，但在移动端被下方列表层覆盖，表现为点击排序按钮后菜单看起来“没出现”。

## 本次 Diff 总结

1. 在 `tag-all` 移动端样式中，为 `.tag-page-function-wrap` 增加 `position: relative` 和 `z-index: 48`，提升排序功能区层级，确保排序浮层显示在列表上方。
2. 在 `attribute-all` 移动端样式中，为 `.attribute-header-function-wrap` 增加 `position: relative` 和 `z-index: 48`，同样修复排序浮层被内容遮挡问题。
3. 同步修改了 `.less` 与 `.css` 文件，保持源码与编译产物一致。

## 涉及文件

- `css/tag-all.less`
- `css/tag-all.css`
- `css/attribute-all.less`
- `css/attribute-all.css`
- `codex_tasks/TASKS.MD`
