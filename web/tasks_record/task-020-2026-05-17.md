# Task 020 - 2026-05-17

## 任务信息
- 任务：`resource-detail.html` 手机版（锚点浮动按钮 + 右侧抽屉目录）
- 目标：移动端补齐与 `post-detail.html` 一致的锚点入口与目录抽屉交互，按钮只显示圆形图标，点击目录标题可跳转对应内容位置。

## 修改文件
- `src/pages/resource-detail.html`
- `resource-detail.html`
- `js/detail/resource-detail.js`
- `css/detail/resource-detail.less`
- `css/detail/resource-detail.css`
- `codex_tasks/TASKS.md`

## Diff 摘要
1. 页面接入锚点入口组件
- 在 `resource-detail` 页面中引入 `detail-page-menu` 组件。
- 为目录容器补齐 `id` 与 `aria-hidden`，按钮通过 `aria-controls` 关联目录面板。

2. 移动端目录交互补齐
- `resource-detail.js` 增加与 `post-detail` 同逻辑的抽屉开关：
  - 按钮点击开关目录（`is-open`）
  - 遮罩点击关闭
  - 关闭按钮点击关闭
  - 同步 `body.modal-open` 与 `aria-expanded/aria-hidden`
- 目录项点击后在移动端先关闭抽屉，再执行锚点滚动跳转。

3. 目录与按钮样式对齐
- 在 `resource-detail.less/.css` 中补齐移动端抽屉样式：遮罩层、右侧抽屉宽高、关闭按钮位置、目录项移动端字号与圆点。
- 按你反馈进行兼容修正后，最终将按钮与抽屉实现对齐为 `post-detail` 同款代码风格（56x56 圆形按钮、同开关显示逻辑）。

4. 缓存规避与构建
- 给 `resource-detail` 引用的 `css/js` 增加版本号查询参数，避免手机端缓存旧资源。
- 执行 `node scripts/build-pages.js`，同步生成根目录页面产物。

## 结果
- `resource-detail.html` 手机版锚点按钮与右侧目录抽屉已完成并通过你确认。
- 本任务在 `codex_tasks/TASKS.md` 已标记为已完成。
