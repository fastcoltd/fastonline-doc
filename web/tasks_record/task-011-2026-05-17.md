# Task 011 - 2026-05-17

## 任务信息
- 任务：`item-detail.html` 手机版（对应 `node-id=722-21957`）
- 目标：按 Figma 调整 Store 卡片中标题与 Hot/Top 标志布局，并确保内容超出卡片展示区域时被卡片裁剪

## 修改文件
- `src/pages/item-detail.html`
- `css/detail/item-detail.less`
- `css/detail/item-detail.css`
- `item-detail.html`（由 `node scripts/build-pages.js` 生成）
- `codex_tasks/TASKS.md`

## Diff 摘要
1. Store 标题区域结构调整
- 在 `src/pages/item-detail.html` 中将标题行改为可控容器：
  - 新增 `item-detail-store-title-row`
  - 新增 `item-detail-store-title-badges`
  - 标志改为 `HOT` 与 `Top 1`
- 评分区域改为复用项目现有组件：
  - `rating-with-count`（移动端自动使用单颗星样式）

2. 手机版样式修正（仅移动端）
- 在 `css/detail/item-detail.less/.css` 中新增与调整 Store 区域样式：
  - 标题字号与行高下调，贴近 Figma
  - 标题行（标题+标志）整体单行约束与裁剪
  - 标志容器与标志文本的收缩/截断规则
  - 卡片容器链路（`#Store .page-section` 到标题子容器）统一 `overflow: hidden`
  - 确保超出卡片区域内容被卡片裁剪

3. 缓存规避
- 更新 `item-detail.css` 引用版本号：`v=20260517-qty6`，避免样式缓存导致页面不更新

4. 构建与验证
- 执行：`node scripts/build-pages.js`
- 构建结果：`Built 27 page(s) to project root.`
- 按用户要求调用 chrome mcp 查看运行效果并迭代调整

## 结果
- 任务已完成并经用户确认。
