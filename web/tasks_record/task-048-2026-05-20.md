# Task 048 - 2026-05-20

## 任务
`fastesp-service.html` 手机版补齐右侧锚点能力：增加浮动入口按钮，点击后展示右侧目录抽屉，并支持点击标题跳转到对应内容位置。

## 修改页面
- `src/pages/fastesp-service.html`
- `fastesp-service.html`（由 `node scripts/build-pages.js` 生成）
- `codex_tasks/TASKS.md`

## 变更摘要
- 仅做最小化改动，且仅针对手机版，不影响 PC 端现有布局和功能。
- 复用现有组件与交互逻辑（与 `post-detail`/`service` 方案一致），未新增重复 CSS 组件。
- 在 `fastesp-service` 页面补齐移动端锚点接入条件：
  - 页面头部容器补 `id="stickyHeader"`，保证 `js/detail/service.js` 运行环境完整；
  - 目录容器补 `id="service-toc-panel"` 与 `aria-hidden`；
  - 页面底部挂载 `detail-page-menu` 浮动按钮 include，并通过 `aria-controls` 关联目录抽屉。
- 已按源码构建规则同步根目录页面产物。
- 已将 `TASKS.md` 对应任务状态更新为“已完成”。

## 影响范围
- 仅影响 `fastesp-service.html` 手机版目录入口与目录抽屉交互。
- 其它页面与 PC 端逻辑不变。
