# Task 046 - 2026-05-20

## 任务
`demand-all.html` 手机版调整：Horizontal 模式下的 item 左右边距减半，并修复卡片左右边缘裁切问题。

## 修改页面
- `css/demand.less`
- `css/demand.css`
- `demand-all.html`（由 `node scripts/build-pages.js` 生成）
- `codex_tasks/TASKS.md`

## 变更摘要
- 仅做最小化改动，且仅针对手机版 Horizontal 模式，不影响 PC 端。
- 调整 `demand-all-horizontal-mobile-card` 宽度规则，使卡片更贴近屏幕边缘并满足“左右间距减少一半”的需求：
  - `width` 从 `calc(100% - 3.2rem)` 调整为 `calc(100% - 0.4rem)`；
  - `margin` 维持 `0 auto`，避免边框被裁切。
- 中途发现手机端缓存导致样式未及时生效，按项目规则执行构建刷新防缓存版本。
- 执行 `node scripts/build-pages.js` 后，同步更新根目录页面产物；本任务验证时 `demand-all.html` 中 `css/demand.css` 版本号更新为 `v=20260520090217`。
- 已将 `TASKS.md` 对应任务状态更新为“已完成”。

## 影响范围
- 仅影响 `demand-all.html` 页面手机版 Horizontal 卡片的左右边距与边缘显示。
- 其它页面功能与 PC 端样式不受影响。
