# Task 012 - 2026-05-17

## 任务信息
- 任务：`item-detail.html` 手机版（`image-12.png` -> `image-13.png`）
- 目标：将 Reviews 区域 `Show More Reviews` 按钮从整行宽改为窄宽样式，并匹配参考图的宽高与圆角

## 修改文件
- `src/pages/item-detail.html`
- `css/detail/item-detail.less`
- `css/detail/item-detail.css`
- `item-detail.html`（由 `node scripts/build-pages.js` 生成）
- `codex_tasks/TASKS.md`

## Diff 摘要
1. 手机版按钮宽高样式修正（仅移动端）
- 在 `css/detail/item-detail.less/.css` 的 `@media (max-width: 768px)` 中调整 `.load-more`：
  - `width: 27.375rem`
  - `flex: 0 0 27.375rem`
  - `max-width: 100%`
  - `align-self: flex-start`
  - `height: 3.75rem`
  - `border-radius: 1.5rem`
- 目的：防止按钮在父级 flex 布局下被拉伸成整行，并保持目标窄宽展示。

2. 样式缓存规避
- 将 `src/pages/item-detail.html` 中样式版本号更新为 `css/detail/item-detail.css?v=20260517-qty8`。

3. 构建与验证
- 执行：`node scripts/build-pages.js`
- 结果：`Built 27 page(s) to project root.`
- 按要求调用 chrome mcp 复核：
  - 选中 `item-detail.html` 页面并切换移动端视口；
  - 实测 `#load-more` 计算样式：`width=219px`、`height=30px`、`borderRadius=12px`、`flex=0 0 219px`。

## 结果
- 任务已完成并经用户确认。
