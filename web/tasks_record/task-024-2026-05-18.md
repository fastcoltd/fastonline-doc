# Task 024 - 2026-05-18

## 任务信息
- 任务：`store-detail.html` 手机版顶部区域间距修复
- 目标：修复指定区域在移动端的左侧边距与其到下方内容间距问题，参考 `item-all.html` 手机版同位置表现，且不影响 PC 端。

## 修改文件
- `css/detail/store-detail.less`
- `css/detail/store-detail.css`
- `codex_tasks/TASKS.md`

## Diff 摘要
1. 移动端面包屑容器对齐（仅手机版）
- 在 `store-detail.less/css` 的 `@media screen and (max-width: 768px)` 中补齐 `.page-top-bread` 规则：
  - `width: 100%`
  - `margin-left: 0`
  - `transform: none`
  - `padding: 0`
  - `box-sizing: border-box`
- 使其与 `item-all.html` 同位置的移动端布局策略一致，修复左侧距离边框偏差。

2. 移动端上下间距修复（仅手机版）
- 在移动端 `.page-content` 增加 `padding-top: 0.625rem`。
- 修复该区域与下方内容的间距不一致问题。

3. 任务状态更新
- 将 `codex_tasks/TASKS.md` 中第一个未完成任务（`store-detail.html` 手机版间距修复）状态更新为“已完成”。

## 校验
- 变更范围校验：仅修改目标任务相关文件，且仅增加移动端样式规则，不涉及 PC 样式。
- 样式语法校验：新增规则为标准 CSS 属性声明，未引入语法错误。

## 结果
- `store-detail.html` 手机版指定区域间距问题已完成修复。
- 任务列表已同步更新状态为“已完成”。
