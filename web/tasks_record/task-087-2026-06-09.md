# Task 087：store-detail.html Reviews 加载更多评论

## 任务内容

store-detail.html 页面手机版和 PC 版中，点击 `Show More Reviews` 按钮以后，需要增加展示一条评论内容。手机版展示效果参考 image-122.png，PC 版展示效果参考 image-123.png。

## 修改页面

- store-detail.html

## 修改内容

- 在 `src/partials/components/store-detail-review-item.html` 的评论组件根节点增加可传入的额外 class，便于复用组件并控制新增评论的默认隐藏状态。
- 在 `src/pages/store-detail.html` 的 Reviews 列表中复用现有评论组件，新增一条默认隐藏的评论项。
- 在 `css/detail/store-detail.less` 和 `css/detail/store-detail.css` 中增加 `store-detail-extra-review` 的隐藏和显示状态。
- 在 `js/detail/store-detail.js` 中新增 `initializeReviewsShowMore`，点击 `Show More Reviews` 后显示新增评论并隐藏按钮。
- 执行页面构建，更新根目录静态 HTML 的防缓存版本。

## 验证

- 执行 `node scripts/build-pages.js`，构建成功，防缓存版本更新为 `20260609082542`。
- 执行 `node --check js/detail/store-detail.js`，语法检查通过。
- 确认根目录 `store-detail.html` 已包含默认隐藏的 `store-detail-extra-review` 评论，并引用新版 `store-detail.css` 和 `store-detail.js`。
