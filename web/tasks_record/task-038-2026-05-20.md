# Task 038 - 2026-05-20

## 任务
`index.html` 手机版 `Demands` 区块的卡片展示不正确，需参考 Figma `node-id=263:1947` 调整为一致效果。

## 修改页面
- `css/index.less`
- `css/index.css`
- `codex_tasks/TASKS.md`
- `index.html`（由 `node scripts/build-pages.js` 生成）
- 其他根目录 `*.html`（统一防缓存版本号刷新，由构建脚本生成）

## 变更摘要
- 仅针对移动端（`max-width: 768px`）调整 `#popuar-demands .figma-popular-demand-item` 的卡片样式：
  - 收敛卡片尺寸、间距、圆角、阴影与文字规格；
  - 调整标题区、头像组、标签区、按钮区布局与尺寸；
  - 重排信息行顺序并隐藏移动端不需要字段，贴近 Figma 卡片结构。
- 修复 `BID NOW` 下方留白：
  - 将移动端卡片最小高度从固定值改为 `min-height: 0;`，避免按钮下方空白。
- 处理缓存问题：
  - 使用 `node scripts/build-pages.js` 重建根目录页面，刷新 `css/js` 资源版本号（`?v=...`），确保手机端拉取最新样式。
- 已将任务状态在 `TASKS.md` 中更新为“已完成”。

## 影响范围
- 仅影响 `index.html` 手机版 `Demands` 卡片展示。
- PC 端样式与交互不受影响。
