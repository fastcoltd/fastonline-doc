# Task 107 - 多个页面的手机版文章正文对齐

## 任务内容

esource-detail.html、fastesp-service.html、service.html，三个页面的手机版，都需要进行相同的修改：文章的正文部分，左侧需要和顶部面包屑的左侧对齐，右侧距离边框的距离需要与左侧相同。

说明：项目实际页面为 `resource-detail.html`。

## 修改页面

- `resource-detail.html`
- `fastesp-service.html`
- `service.html`

## Diff 总结

- 调整 `css/detail/resource-detail.less` 与 `css/detail/resource-detail.css` 中移动端 `.article-content` 的左右 padding，使文章正文与现有面包屑左侧对齐，并保留底部间距。
- 调整 `css/detail/service.less` 与 `css/detail/service.css` 中移动端 `.article-content` 的左右 padding，覆盖 `fastesp-service.html` 与 `service.html` 的正文对齐。
- 更新 `src/pages/resource-detail.html`、`src/pages/fastesp-service.html`、`src/pages/service.html` 的详情样式防缓存版本。
- 运行 `node scripts/build-pages.js` 生成根目录 `resource-detail.html`、`fastesp-service.html`、`service.html`，刷新静态页面缓存版本。

## 校验

- 已执行 `git diff --check`，无空白或语法检查问题。
