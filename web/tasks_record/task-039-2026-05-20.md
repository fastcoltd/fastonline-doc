# Task 039 - 2026-05-20

## 任务
多个页面手机版排序按钮样式修复：`demand-all.html` 排序按钮过小且右侧缺少间距，`compaign-all.html`、`store-all.html`、`item-all.html` 同步按 `attribute-all.html` 的筛选按钮风格调整。

## 修改页面
- `css/item-all.less`
- `css/item-all.css`
- `css/demand.less`
- `css/demand.css`
- `src/pages/demand-all.html`
- `demand-all.html`（由 `node scripts/build-pages.js` 生成）
- `codex_tasks/TASKS.md`

## 变更摘要
- 仅针对移动端（`max-width: 768px`）调整排序按钮样式：
  - 将排序图标从 `2rem` 统一为 `3rem`，修复按钮过小问题；
  - 为排序按钮补充 `padding-right: 0.5rem`，增加右侧间距。
- 修复 `demand-all.html` 未生效问题：
  - 在 `src/pages/demand-all.html` 的筛选/排序头部容器补充 `item-all-filter-layoutcontrol-box` 类，使通用移动端样式命中。
- 按源码构建规则执行 `node scripts/build-pages.js` 同步生成根目录 `demand-all.html`。
- 已将 `TASKS.md` 中本任务状态更新为“已完成”。

## 影响范围
- 仅影响 `demand-all.html`、`compaign-all.html`、`store-all.html`、`item-all.html` 的手机版排序按钮展示。
- PC 端样式与交互不受影响。
