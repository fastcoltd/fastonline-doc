# Task 058 - 2026-05-23

## 任务
`store-detail.html` 手机版在 `FAQs` 上方增加 `Show More` 按钮，点击后新增显示 2 条 `Items` 数据，并对齐按钮与上方内容间距。

## 修改页面
- `src/pages/store-detail.html`
- `css/detail/store-detail.less`
- `css/detail/store-detail.css`
- `js/detail/store-detail.js`
- 根目录 `store-detail.html` 产物（`node scripts/build-pages.js` 生成）
- `codex_tasks/TASKS.md`

## 变更摘要
- 仅修改手机版，不影响 PC。
- 在 `Items` 区块底部（`FAQs` 上方）复用已有 `show-more-button` 组件，新增 `Show More` 按钮。
- 预置 2 条额外 `Items` 卡片数据并默认隐藏；点击按钮后显示这 2 条数据，并隐藏按钮。
- 新增移动端展示逻辑（`js/detail/store-detail.js`）：
  - 仅移动端启用按钮交互；
  - 点击按钮后为额外卡片添加显示类。
- 同步样式到 `less/css`：
  - 额外卡片默认隐藏，激活后显示；
  - `Show More` 按钮仅移动端显示；
  - 按钮顶部间距调整为 `15px`；
  - 去除移动端 `Items` 卡片额外 `margin-bottom`，避免与按钮间距叠加导致视觉偏大。
- 执行 `node scripts/build-pages.js`，更新根目录 `store-detail.html` 与防缓存版本号（`v=20260523092302`）。
- 将 `TASKS.md` 对应任务状态更新为“已完成”。

## 影响范围
- 仅影响 `store-detail.html` 手机版 `Items` 区块与其上方 `Show More` 按钮交互。
- 不影响其他页面及 PC 端布局。
