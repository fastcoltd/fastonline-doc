# Task 130 - header 登录后 PC 版金币图标隐藏

**日期**: 2026-06-25
**状态**: 已完成
**页面**: 全局 header，影响所有引用 `site-header.html` / `common.css` 的静态页面

## 任务内容
登录成功后，右上角会有金币图标，需要去掉这个图标，同时其他内容的布局不能发生改变。由于 header 是通用组件，修改后整个项目中的 header 都需要是修改后的效果。

## 修改内容

### 1. `css/common.less`
- 在 `.header-user-box .icon-jinbi1` 中增加 `visibility: hidden;`
- 保留原本 `3rem` 宽高和 flex 占位，避免通知图标、头像等其他内容发生位移

### 2. `css/common.css`
- 同步增加 `.header-user-box .icon-jinbi1 { visibility: hidden; }`

### 3. 根目录静态页面
- 运行 `node scripts/build-pages.js`
- 重新生成 30 个根目录 HTML
- 资源防缓存版本更新为 `20260625144604`

## 检查结果
- 已执行 `node --check scripts/build-pages.js`，语法检查通过
- 未调用 chrome mcp，按任务规则由用户自行确认效果

## 影响范围
- 登录态 PC 版 header 的金币图标不再显示
- 原图标位置保留占位，避免 header 登录态布局变化
- 通用 header 生效，因此所有引用全局 header 的页面同步生效
