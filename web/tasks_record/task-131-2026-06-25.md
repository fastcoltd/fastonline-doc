# Task 131 - header 登录后 PC 版浮窗顶部余额信息

**日期**: 2026-06-25
**状态**: 已完成
**页面**: 全局 header 登录后账号浮窗，影响所有引用 `common.css` 的静态页面

## 任务内容
登录成功后，点击右上角头像出现账号浮窗。浮窗顶部需要在邮箱下方增加余额信息，字体和图标需要与邮箱区域协调，其中图片从手机版登录后点击头像跳转的页面中获取。

## 修改内容

### 1. `css/common.less`
- 显示 PC 版账号浮窗顶部原有的 `.account-data-wrapper`
- 复用移动端账号中心的 `image/account-center-mobile/money-icon.svg` 作为余额图标
- 将余额文字调整为与邮箱一致的 `1.5rem` 字号和 `2.25rem` 行高
- 隐藏顶部数据区中的通知项，仅展示余额
- 将顶部区域高度调整为 `7.5rem`，为新增余额行留出空间
- 根据后续反馈，将列表区域顶部间距从 `2.5rem` 调整为 `1.25rem`，缩小余额行下方留白

### 2. `css/common.css`
- 同步上述样式修改，保持源码与编译产物一致

### 3. 根目录静态页面
- 运行 `node scripts/build-pages.js`
- 重新生成 30 个根目录 HTML
- 资源防缓存版本更新为 `20260625150004`

## 检查结果
- 已执行 `node --check scripts/build-pages.js`，语法检查通过
- 未调用 chrome mcp，按任务规则由用户自行确认效果

## 影响范围
- PC 版登录后账号浮窗顶部邮箱下方新增余额信息
- 余额行下方间距已缩小
- 全局 header 浮窗生效，因此所有引用全局账号浮窗的页面同步生效
