# Task 125：页面的pc版需要修改

## 任务内容

Best items 中的每一个 item，其中的标题部分需要修改：

1. 高度只展示 1 行。
2. 标签 + 标题只展示 1 行。
3. 优先展示标签，标题展示不下的部分显示省略号。

## 修改页面

- `index.html`
- `item-detail.html`
- `post-detail.html`
- `system-post-detail.html`
- `store-detail.html`

## Diff 总结

- 调整首页 `Best items` 卡片标题样式，将标题容器高度从两行的 `10rem` 改为单行 `5rem`。
- 将标题链接改为单行 flex 布局，标签设置为 `flex: 0 0 auto`，保证优先展示标签。
- 将标题文本设置为 `min-width: 0`、`white-space: nowrap`、`overflow: hidden`、`text-overflow: ellipsis`，超出时显示省略号。
- 同步更新通用 `items` 样式，以及 `item-detail`、`post-detail`、`system-post-detail`、`store-detail` 中复用/复刻的 Best items PC 样式。
- 同步维护 `.less` 与 `.css` 文件。
- 执行 `node scripts/build-pages.js`，刷新根目录 HTML 的防缓存版本。

## 验证

- `node scripts/build-pages.js`
- `git diff --check`
- `node --check scripts/build-pages.js`

