# Task 052 - 2026-05-22

## 任务
`compaign-detail.html` 手机版中 `Intelligent Soft` 下方 item，需要改为与 `compaign-all.html` 的 Horizontal display 模式一致，并复用同一组件。

## 修改页面
- `src/pages/compaign-detail.html`
- `css/detail/compaign-detail.less`
- `css/detail/compaign-detail.css`
- `codex_tasks/TASKS.md`
- 根目录 `compaign-detail.html` 产物（由 `node scripts/build-pages.js` 重新生成，含缓存版本号更新）

## 变更摘要
- 仅针对手机版生效，不影响 PC。
- `section-3`（`Intelligent Soft` 区块）列表容器增加专用类 `compaign-detail-horizontal-list-compaign`，用于只覆盖该区块的横向卡片样式。
- 在 `css/detail/compaign-detail.less` 与 `css/detail/compaign-detail.css` 中新增该专用类的移动端规则，使 `compaign-item-icon` 与 `compaign-item-content` 的宽度参数对齐 `compaign-all` Horizontal display。
- 修复了首次绑定位置错误：将专用类从 `section-2` 更正到 `section-3`，确保目标区块样式正确命中。
- 执行 `node scripts/build-pages.js`，更新根目录产物和缓存版本。
- 将 `TASKS.md` 对应任务状态更新为“已完成”。

## 影响范围
- 影响 `compaign-detail.html` 手机版 `Intelligent Soft` 区块下方横向 item 展示。
- 不影响 PC 布局和其他页面业务逻辑。
