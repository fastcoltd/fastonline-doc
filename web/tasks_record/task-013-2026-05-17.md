# Task 013 - 2026-05-17

## 任务信息
- 任务：`item-detail.html` 手机版 Similar items 每页展示数量调整
- 目标：将 Similar items 从每页显示 4 条改为每页显示 2 条，效果对齐 Figma（`node-id=998:38589`）

## 修改文件
- `css/detail/item-detail.less`
- `css/detail/item-detail.css`
- `codex_tasks/TASKS.md`

## Diff 摘要
1. 仅调整移动端可见数量规则
- 在 `@media screen and (max-width: 768px)` 中新增：
  - `.item-detail-pager #best-items .best-items-item:nth-child(n + 3) { display: none; }`
- 作用：每个轮播页仅显示前 2 个卡片，第 3 个及之后隐藏。

2. 同步 less/css
- 同步修改 `item-detail.less` 与 `item-detail.css`，保持源码与产物一致。

3. 任务状态维护
- 将 `codex_tasks/TASKS.md` 中该任务状态从“未完成”更新为“已完成”。

## 结果
- 仅影响 `item-detail` 手机版 Similar items 展示数量，未改动 PC 布局和其他模块。
