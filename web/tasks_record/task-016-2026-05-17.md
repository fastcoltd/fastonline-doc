# Task 016 - 2026-05-17

## 任务
`post-detail.html` 手机版 Reviews 模块评分组件改为单星样式，优先复用现有组件。

## 修改页面
- `post-detail.html`

## Diff 总结
- 在 Reviews 区块的两个评论项中，将原评分容器从 `item-star-box` 调整为 `item-star-box item-star-box-pc`，使其仅用于 PC 展示。
- 在同位置新增移动端评分组件 include：`rating-with-count-mobile`，复用项目现有单星评分结构（星标 + 分数 + 数量）。
- 参数设置为 `score: 5.0`、`recommend: (62)`，与当前示例数据一致。
- 未改动 PC 布局逻辑，仅对移动端展示生效。

## 变更文件
- `src/pages/post-detail.html`
- `post-detail.html`（由 `node scripts/build-pages.js` 生成）
