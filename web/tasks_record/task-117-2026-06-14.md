# Task 117 - item-detail.html 页面手机版 description 卡片间距调整

## 任务内容

item-detail.html 页面手机版中，description 部分文本距离卡片边缘的间距太大，需要改成与 Screenshots 部分图片距离卡片边缘的间距一致。

## 修改页面

- `item-detail.html`

## Diff 总结

- 在 `css/detail/item-detail.less` 的移动端媒体查询中，为 `.item-detail-description-box` 增加 `padding: 1.25rem;`。
- 同步更新编译产物 `css/detail/item-detail.css`，保持 LESS 与 CSS 一致。
- 更新 `src/pages/item-detail.html` 中 `css/detail/item-detail.css` 的版本号，用于防缓存。
- 执行 `node scripts/build-pages.js`，重新生成根目录产物 `item-detail.html`。

## 验证

- `node scripts/build-pages.js`
- `node --check scripts/build-pages.js`
