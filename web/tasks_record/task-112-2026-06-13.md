# Task 112

## 任务

`attribute-all.html` 页面的手机版需要修改：顶部属性标签现在换行显示，需要改成一行显示，展示不下的部分可以滚动展示。

## 修改内容

- 将 `attribute-all.html` 手机版顶部属性标签容器改为横向单行滚动。
- 标签项设置为不收缩，避免被压扁或换行。
- 隐藏横向滚动条，保留触摸横向滑动能力。
- 同步修改 `css/attribute-all.css` 与 `css/attribute-all.less`。
- 执行 `node scripts/build-pages.js` 重新生成根目录页面，并刷新防缓存版本号为 `20260613104341`。

## 涉及文件

- `codex_tasks/TASKS.md`
- `css/attribute-all.css`
- `css/attribute-all.less`
- `attribute-all.html`
- 根目录其他页面：由构建脚本统一刷新资源防缓存版本号

## 验证

- `node scripts/build-pages.js`
- `git diff --check`

