# Task 119：store-detail.html页面的pc版需要修改

## 任务内容

store-detail.html 页面 pc 版中，Sales Count 图表右上角需要增加 7D / 14D / 30D 切换按钮，参考 Overview 部分实现。修改完成后抽取成独立组件并替换到此处，同时检查全局同类展示并替换为组件。

## 修改页面

- `store-detail.html`
- `item-detail.html`

## Diff 总结

- 新增 `src/partials/components/statistics-period-header.html`，统一封装统计区标题和 7D / 14D / 30D 周期切换按钮。
- `src/pages/store-detail.html` 中 Overview 和 Sales Count 头部改为复用统计周期头部组件。
- `src/pages/item-detail.html` 中同类 Overview 和 Sales Count 头部同步替换为该组件。
- `js/detail/store-detail.js` 和 `js/detail/item-detail.js` 中将周期切换逻辑扩展为支持 Overview / Sales Count 两类图表。
- Sales Count 图表补充 7D / 14D / 30D 数据源，点击切换时更新图表数据。
- `css/item-statistics.css` 与 `css/item-statistics.less` 中让 `.sales-header` 与 `.overview-header` 共用 PC 布局，使切换按钮在右侧并与 Overview 对齐；移动端仍保持宽度 100%。
- 更新 `item-statistics.css`、`store-detail.js`、`item-detail.js` 的页面引用版本号，保证防缓存。
- 执行 `node scripts/build-pages.js` 重新生成根目录 HTML 产物。

## 验证

- `node --check js/detail/item-detail.js`
- `node --check js/detail/store-detail.js`
- `node scripts/build-pages.js`
