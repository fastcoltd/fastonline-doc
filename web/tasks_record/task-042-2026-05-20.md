# Task 042 - 2026-05-20

## 任务
`blog.html` 手机版修复：顶部布局、顶部悬停区域表现与面包屑布局对齐 `item-all.html`。

## 修改页面
- `src/pages/blog.html`
- `css/detail/blog.less`
- `css/detail/blog.css`
- `blog.html`（由 `node scripts/build-pages.js` 生成）
- `codex_tasks/TASKS.md`

## 变更摘要
- 仅针对该任务做最小改动，聚焦手机版展示，不影响 PC 端其它功能。
- 将 `blog` 页顶部容器由 `page-top-sticky page-top-sticky-main` 调整为 `page-head page-head-main`，与 `item-all.html` 的顶部结构保持一致。
- 在移动端媒体查询（`max-width: 768px`）补齐 `page-top-bread` 对齐规则：
  - `width: 100%`
  - `margin-left: 0`
  - `transform: none`
  - `padding: 0`
  使面包屑位置与 `item-all.html` 手机版一致。
- 已同步维护 `less/css` 两份样式文件一致。
- 按源码构建规则执行 `node scripts/build-pages.js`，同步生成根目录 `blog.html`，并刷新防缓存版本号（`20260520033905`）。
- 已将 `TASKS.md` 中该任务状态更新为“已完成”。

## 影响范围
- 仅影响 `blog.html` 页面手机版顶部与面包屑布局表现。
- 其它页面与 PC 端不受影响。
