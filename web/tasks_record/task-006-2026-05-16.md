# Task 006 完成记录

- 任务：`codex_tasks/TASKS.md` - Task（`post-detail.html` 手机版顶部信息区，参考 Figma `node-id=852:28617`）
- 完成日期：2026-05-16
- 修改范围：仅 `post-detail` 页面手机版顶部信息模块，不影响 PC 端与其他页面

## 本次 Diff 总结

1. 读取 Figma 设计稿并按节点参数对齐顶部信息区：
   - 目标节点：`852:28617`（标题、标签、Blog/Store、评分、图标、Reads、标签）。

2. 调整移动端结构与顺序，保证评分行布局与设计一致：
   - 将评分组件从 `info-section` 移到 `reads-tag-wrapper` 的 `attribute-icon-wrapper` 内；
   - 实现“评分 + 图标 + Reads/Paid”同一行展示。

3. 调整移动端样式细节（仅 `@media (max-width: 768px)`）：
   - 顶部标题区、收藏按钮位置、HOT 标签间距与尺寸；
   - `attribute-icon-wrapper` 与 `reads-tag-wrapper` 的间距与对齐；
   - 图标尺寸、标签胶囊尺寸与文字行高。

4. 源码与产物同步：
   - 按规则执行 `node scripts/build-pages.js`，同步更新根目录 `post-detail.html` 产物。

## 涉及文件

- `src/pages/post-detail.html`
- `css/detail/post-detail.less`
- `css/detail/post-detail.css`
- `post-detail.html`
- `codex_tasks/TASKS.md`
