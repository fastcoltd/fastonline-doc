# Task 057 - 2026-05-23

## 任务
`store-detail.html` 手机版将指定 `Items` 卡片区域改成复用 `index.html` 的 `Best items` item 样式，要求最终展示一致。

## 修改页面
- `src/pages/store-detail.html`
- `css/detail/store-detail.less`
- `css/detail/store-detail.css`
- 根目录 `store-detail.html` 产物（`node scripts/build-pages.js` 生成）
- `codex_tasks/TASKS.md`

## 变更摘要
- 仅修改手机版，不影响 PC。
- 修正复用样式作用域命中问题：将 `#best-items` 放到正确父容器，确保 `items.css` 中 `#best-items .layout-vertical ...` 的移动端规则生效。
- 同步对齐移动端卡片布局参数：卡片宽度改为 `calc((100% - 1rem) / 2)`，列表间距改为 `1rem`，并使用 `justify-content: flex-start`。
- 首个卡片的标签结构对齐 `index` 参考项（复用同类文案样式类），保证显示细节一致。
- 执行 `node scripts/build-pages.js` 生成根目录 `store-detail.html`，并更新防缓存版本号（`v=20260523062548`）。
- 将 `TASKS.md` 对应任务状态更新为“已完成”。

## 影响范围
- 仅影响 `store-detail.html` 手机版 `Items` 卡片区展示。
- 不影响其他页面与 PC 端布局逻辑。
