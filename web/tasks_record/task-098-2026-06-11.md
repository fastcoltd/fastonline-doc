# Task 098：search-all.html Demands 分类 item 标题与收藏按钮调整

## 任务内容

`search-all.html` 页面 Demands 分类 item：

- 收藏/取消收藏按钮需要与标题第一行纵向居中。
- 标题只展示一行，超出省略。
- 收藏按钮不能遮挡右上角标签。
- 标题不能和收藏按钮重合，最小间距 20px。

## 修改摘要

- 修改 `css/search-all.less` 和 `css/search-all.css`：
  - 新增 PC 端 `.search-card-fifth-item-desktop` 专用标题行规则。
  - 标题区域右侧预留空间，避免收藏按钮与右上角状态标签重合。
  - 标题改为单行省略。
  - 收藏按钮与标题第一行纵向居中。

- 重新构建 `search-all.html`：
  - 防缓存版本刷新为 `20260611081642`。

## 验证

- 已执行 `node scripts/build-pages.js`，构建成功。
- 已执行 `git diff --check`，无格式/空白错误。
- 本任务未要求调用 chrome mcp，未进行浏览器确认。
- 已发送飞书通知。
