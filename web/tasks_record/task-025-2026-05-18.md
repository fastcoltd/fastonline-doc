# Task 025 - 2026-05-18

## 任务信息
- 任务：`store-detail.html` 手机版整体布局按 Figma（`node-id=998:42164`）调整
- 目标：仅修改手机版布局，对齐设计稿，不影响 PC 端。

## 修改文件
- `css/detail/store-detail.less`
- `css/detail/store-detail.css`
- `js/detail/store-detail.js`
- `src/pages/store-detail.html`
- `store-detail.html`（由构建脚本生成）
- `codex_tasks/TASKS.md`

## Diff 摘要
1. 手机版布局与样式对齐
- 调整 Items 卡片两列布局、筛选区排版、右侧分组标题/间距、模块间距等移动端样式。
- 隐藏移动端不需要的顶部 `page-link` 导航，保持页面顺序式展示。

2. 滚动异常修复（手机版）
- 修复“打开后自动跳到 Overview、且滚动回弹”的问题：
  - 仅在 `page-link` 可见且非移动端时初始化 `LinkRef`，避免隐藏态触发 `scrollIntoView`。
  - 移动端且 `page-fix-box` 无 `data-visible` 时，清理残留桌面端内联定位样式（`position/left/top/transform/maxHeight/height`），解除位置锁定与回弹。
  - 移动端判定升级为“宽度 + coarse pointer”双判定，提高安卓机型兼容性。

3. 缓存规避
- 为 `store-detail.js` 增加版本参数：`?v=20260518-compat2`，避免真机缓存旧脚本导致修复未生效。

4. 构建与验收
- 执行 `node scripts/build-pages.js` 同步生成根目录 `store-detail.html`。
- 按要求通过 chrome mcp 复测滚动路径，确认首屏与回滚路径正常。

5. 任务状态更新
- 将 `codex_tasks/TASKS.md` 中对应任务状态更新为“已完成”。

## 结果
- `store-detail.html` 手机版布局已按设计稿完成调整。
- 手机版滚动异常已修复并完成复测。
