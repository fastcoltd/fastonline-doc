# Task 063 - 2026-05-24

## 任务
`search-all.html` 手机版中，`Demands` tab 的 item 改为复用 `demand-all.html` 的 Horizontal 模式 item 组件，并修正收藏按钮位置（重点兼容一加 Ace5 Pro）。

## 修改页面
- `src/partials/components/search-all-demand-item.html`
- `src/pages/search-all.html`
- `css/search-all.less`
- `css/search-all.css`
- 根目录 `search-all.html` 产物（`node scripts/build-pages.js` 生成）
- `codex_tasks/TASKS.md`

## 变更摘要
- 仅修改手机版展示逻辑，PC 保持原有 `Demands` 卡片结构。
- 在 `search-all` 的 `Demands` 卡片中：
  - 保留原有结构用于 PC（`search-card-fifth-item-desktop`）；
  - 新增移动端复用结构（`search-card-fifth-item-mobile`），直接复用 `demand-all-horizontal-mobile-item` 组件。
- 为保证复用组件样式完整，在 `search-all` 页面补充引入 `demand.css`。
- 同步样式到 `less/css`：
  - 新增 `search-card-fifth-item-mobile` 默认隐藏规则；
  - 在 `max-width: 768px` 下隐藏桌面结构、显示移动结构；
  - 修正收藏按钮定位为移动端右上角（`top: 0.75rem; right: 1.25rem; width/height: 2.75rem`）。
- 执行 `node scripts/build-pages.js`，更新根目录页面与防缓存版本号（`v=20260524011617`）。
- 将 `TASKS.md` 对应任务状态更新为“已完成”。

## 影响范围
- 仅影响 `search-all.html` 手机版 `Demands` tab 的 item 展示与收藏按钮定位。
- 不影响 PC 端及其他页面功能。
