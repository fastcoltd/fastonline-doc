# Task 021 - 2026-05-17

## 任务信息
- 任务：`resource-detail.html` 手机版（Other Pages 模块）
- 目标：按 Figma（`node-id=998:41973`）调整为每页仅 1 行，且每行展示 2 个 item。

## 修改文件
- `js/detail/resource-detail.js`
- `src/pages/resource-detail.html`
- `resource-detail.html`
- `codex_tasks/TASKS.md`

## Diff 摘要
1. 移动端轮播重排逻辑（仅手机版）
- 在 `resource-detail.js` 新增 `optimizeOtherPagesForMobile()`。
- 仅在 `max-width: 768px` 或触屏设备执行，不影响 PC。
- 将 `#best-items` 的 `resource-page-item` 重分组为每页 `2` 个（每页 1 行、每行 2 个）。
- 重建轮播指示点数量，保证分页与分组一致。
- 在 `DOMContentLoaded` 中先执行重排，再初始化 `new Carousel('best-items', 20)`。

2. 资源版本号更新（防缓存）
- `src/pages/resource-detail.html` 中将 `resource-detail.css/js` 引用升级到 `v=20260517-6`。
- 执行 `node scripts/build-pages.js` 同步根目录产物 `resource-detail.html`。

## Chrome MCP 验证
- 使用移动端视口 `375x812` 进行验证。
- 页面加载资源：`resource-detail.css?v=20260517-6`、`resource-detail.js?v=20260517-6`。
- 轮播页内卡片数量：`counts = [2,2,2,2,2,2]`（每页 2 个）。
- 首屏两卡片同排：`firstTwoOnSameRow = true`。
- 卡片宽度：`166.5px`、`166.5px`，与 Figma 节点布局一致。
- 验证截图：`/private/tmp/resource-detail-other-pages-mobile-v6.png`。

## 结果
- `resource-detail.html` 手机版 Other Pages 已按要求实现“每页 1 行、每行 2 个 item”。
- `codex_tasks/TASKS.md` 对应任务已标记为已完成。
