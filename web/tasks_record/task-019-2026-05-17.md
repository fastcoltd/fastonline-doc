# Task 019 - 2026-05-17

## 任务
- `resource-detail.html` 手机版：修复 `image-25` 对应区域的面包屑左侧间距与下方内容间距，参考 `item-all.html` 手机版同位置布局。

## 修改内容
- 仅针对手机版进行最小化样式修改，不影响 PC。
- 在 `@media (max-width: 768px)` 下补齐面包屑容器对齐规则（`width/margin-left/transform/padding/box-sizing`），与 `item-all` 同位置一致。
- 将面包屑下方到顶部内容的间距调整为 `5px`（`0.5rem`）。
- 同步修改 `.less` 与 `.css`。
- 按你的要求，在 `AGENTS.md` 增加飞书命令执行约定：统一使用单条脚本命令，避免复合命令导致重复确认。

## 变更文件
- `css/detail/resource-detail.less`
- `css/detail/resource-detail.css`
- `AGENTS.md`
- `codex_tasks/TASKS.md`

## 结果
- `resource-detail` 手机版目标区域间距已按要求调整完成。
- 首个未完成任务状态已标记为已完成。
