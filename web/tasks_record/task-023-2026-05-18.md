# Task 023 - 2026-05-18

## 任务信息
- 任务：`service.html` 手机版锚点目录组件补齐
- 目标：复用已实现的移动端目录浮动按钮与右侧抽屉锚点交互（参考 `post-detail.html` / `resource-detail.html`），并与 Figma 节点 `852:28733`、`997:36993` 对齐。

## 修改文件
- `src/pages/service.html`
- `service.html`
- `js/detail/service.js`
- `css/detail/service.less`
- `css/detail/service.css`
- `codex_tasks/TASKS.md`

## Diff 摘要
1. 页面结构与组件接入
- 在 `src/pages/service.html` 的目录容器补充 `id="service-toc-panel"` 与 `aria-hidden`。
- 接入公共组件 `detail-page-menu`，绑定 `aria-controls="service-toc-panel"`。
- 执行 `node scripts/build-pages.js` 同步根目录产物 `service.html`。

2. 移动端目录交互（仅手机版）
- 重写 `js/detail/service.js` 中菜单交互逻辑：
  - 使用 `is-open` 控制抽屉显隐；
  - 同步 `aria-expanded` / `aria-hidden` 状态；
  - 支持点击遮罩关闭、点击关闭按钮关闭；
  - 移动端点击 `.toc-item` 后关闭抽屉并跳转到对应 section。
- 修复旧逻辑中 `body` 未定义导致的潜在异常。

3. 移动端样式对齐（仅手机版）
- 在 `service.less/css` 新增/同步 `detail-page-menu` 的移动端样式：
  - `56x56`、圆形、红底、图片满铺且圆形裁切。
- 将目录抽屉样式调整为与已完成详情页一致：
  - 遮罩层 `rgba(0,0,0,0.6)`；
  - 右侧抽屉宽度 `calc(100vw - 8rem)`；
  - 圆角卡片、可滚动、底部关闭按钮定位。
- 同步 `privacy-policy-page` 移动端目录宽度与内边距，避免样式冲突。

## 校验
- 语法校验：`node --check js/detail/service.js` 通过。
- 构建校验：`node scripts/build-pages.js` 成功。
- Chrome MCP 移动端（`375x812`）验证通过：
  - 浮动按钮样式符合设计（圆形红底，56x56，图片圆切）；
  - 点击按钮可打开右侧目录抽屉；
  - 点击目录项可关闭抽屉并滚动到目标 section；
  - 点击关闭按钮可关闭抽屉。

## 结果
- `service.html` 手机版锚点目录任务已完成。
- `codex_tasks/TASKS.md` 对应任务状态已更新为“已完成”。
