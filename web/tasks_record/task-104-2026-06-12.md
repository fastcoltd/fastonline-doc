# Task 104：新增brand-service.html页面，需要支持pc版和手机版

## 任务内容

新增/检查 `brand-service.html` 页面，支持 pc 版和手机版。按 Figma 设计稿实现，优先复用现有组件；页面可参考 `item-all.html`，但必须复制隔离实现，不能影响 `item-all.html`。

## 修改页面

- `brand-service.html`

## Diff 总结

- 检查到项目中已存在 `src/pages/brand-service.html`、根目录 `brand-service.html`、`css/brand-service.less` 和 `css/brand-service.css`。
- 使用 Figma MCP 读取 pc 和手机设计节点，尤其是差异区：
  - pc：`732:32903`
  - 手机：`732:32981`、`732:32992`
- 将原来的大品牌介绍卡片替换为 Figma 中的紧凑品牌服务头部：
  - Adobe 图标 + `Adobe`
  - `HOT` 与 `Limited time sale` 标签
  - `Service:` 分类标签行
- 保留列表、筛选、布局切换等从 `item-all` 复制出来的隔离实现，未修改 `item-all.html`。
- 同步修改 `css/brand-service.less` 和 `css/brand-service.css`。
- 通过 `node scripts/build-pages.js` 同步生成根目录产物 `brand-service.html`。
- 将 `codex_tasks/TASKS.md` 中该任务状态标记为 `已完成`。

## 验证

- 已执行 `node scripts/build-pages.js`。
- 已执行 `git diff --check`。
- 飞书通知已发送成功。
