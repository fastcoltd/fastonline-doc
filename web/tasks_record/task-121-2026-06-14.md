# Task 121：compaign-detail.html页面的手机版需要修改

## 任务内容

compaign-detail.html 手机版 item 中增加购买按钮，参考 item-all.html 页面手机版 Horizontal 模式下 item 的购买按钮实现。

## 修改页面

- `compaign-detail.html`

## Diff 总结

- `src/partials/item-all-horizontal-item.html` 改为引用已有的 `item-all-horizontal-item-responsive` 响应式组件，使 compaign-detail 中复用该 partial 的 item 在移动端具备 `BUY NOW` 购买按钮。
- `css/detail/compaign-detail.less` 与 `css/detail/compaign-detail.css` 中仅在移动端规则内切换展示：隐藏桌面横向 item，显示移动端横向 item。
- 执行 `node scripts/build-pages.js` 重新生成根目录 HTML 产物，`compaign-detail.html` 中已生成 `item-all-horizontal-mobile-buy-btn`，并刷新 css/js 防缓存版本号。

## 验证

- `node scripts/build-pages.js`
- 使用 `rg` 确认根目录 `compaign-detail.html` 已生成 `item-all-horizontal-mobile-buy-btn` / `BUY NOW`
