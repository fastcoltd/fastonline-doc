# Task 061 - 2026-05-23

## 任务
`search-all.html` 手机版中，`Posts` tab 的 item 改为复用 `post-all.html` 的 item 组件，并修复复用后标题过大与收藏按钮定位错误问题（重点兼容一加 Ace5 Pro）。

## 修改页面
- `src/pages/search-all.html`
- `src/partials/components/search-all-post-item.html`
- `css/search-all.less`
- `css/search-all.css`
- 根目录 `search-all.html` 产物（`node scripts/build-pages.js` 生成）
- `codex_tasks/TASKS.md`

## 变更摘要
- 仅修改手机版展示逻辑，PC 保持原有 `Posts` 卡片结构。
- 在 `search-all` 的 `Posts` 卡片中：
  - 保留原有结构用于 PC（`search-card-third-item-desktop`）；
  - 新增移动端复用结构（`search-card-third-item-mobile`），直接复用 `post-shared-item` 组件。
- 为保证复用组件样式完整，在 `search-all` 页面补充引入 `post.css`。
- 同步样式到 `less/css`，并做移动端兼容修复：
  - 新增移动端桌面/手机结构显隐切换规则；
  - 将移动端收藏按钮固定为绝对定位（`top: 0.75rem; right: 1.25rem; z-index: 2;`）；
  - 将移动端标题字号与行高覆盖为 `1.75rem / 2.5rem`，避免被通用 `search-card` 规则放大。
- 执行 `node scripts/build-pages.js`，更新根目录页面与防缓存版本号（`v=20260523125452`）。
- 将 `TASKS.md` 对应任务状态更新为“已完成”。

## 影响范围
- 仅影响 `search-all.html` 手机版 `Posts` tab 的 item 展示、标题字号和收藏按钮定位。
- 不影响 PC 端及其他页面功能。
