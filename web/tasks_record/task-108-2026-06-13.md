# Task 108 - footer 组件文字跳转

## 任务内容

footer 中的文字（可以点击的），点击后需要跳转到对应的页面。请在项目中查找合适的页面进行跳转。尽可能每个文字都有对应的页面可以跳转（如果有，没有则保持原样即可）。

## 修改页面

- 全局 footer 组件：`src/partials/components/site-footer.html`
- 构建同步后，所有引用 footer 的根目录静态页面均已更新。

## Diff 总结

- 为 footer 中所有 `.footer-item-text` 文案补充 `href`。
- 根据确认后的范围，将 footer 跳转分配到以下页面：
  - `brand-service.html`
  - `blog.html`
  - `attribute-all.html`
  - `tag-all.html`
  - `faq-detail.html`
  - `system-post-detail.html`
- 运行 `node scripts/build-pages.js`，同步生成根目录静态页面并刷新缓存版本。

## 校验

- 已执行 `git diff --check`，无空白检查问题。
- 已执行 `node --check scripts/build-pages.js`，构建脚本语法检查通过。
- 已确认 `src/partials/components/site-footer.html` 中所有 `.footer-item-text` 均包含 `href`。
