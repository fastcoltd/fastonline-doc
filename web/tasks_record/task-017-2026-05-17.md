# Task 017 - 2026-05-17

## 任务信息
- 任务：`post-detail.html` 手机版（Reviews 模块 `image-21.png` -> `image-22.png`）
- 目标：`Show More Reviews` 按钮按参考图调整大小、间距、边框样式，并完成异常高度修正

## 修改文件
- `css/detail/post-detail.less`
- `css/detail/post-detail.css`
- `codex_tasks/TASKS.md`

## Diff 摘要
1. 手机版按钮样式调整（仅移动端）
- 在 `@media (max-width: 768px)` 中将 `.load-more` 调整为目标窄宽按钮：
  - `width: 27.375rem`
  - `max-width: 100%`
  - `align-self: flex-start`
  - `height: 3.75rem`
  - `border-radius: 1.5rem`

2. 按钮高度异常修正
- 原先设置 `flex: 0 0 27.375rem` 会在纵向 flex 容器中把主轴高度锁死，导致按钮被拉高。
- 修正为 `flex: 0 0 auto`，保留宽度控制，恢复正常按钮高度。

3. 任务状态更新
- 将 `codex_tasks/TASKS.md` 对应任务状态由“未完成”更新为“已完成”。

## 结果
- `post-detail.html` 手机版 Reviews 区块 `Show More Reviews` 按钮样式已按参考图完成。
- 修改范围仅限该任务目标，不影响 PC 端与其他功能。
