# Task 035 - 2026-05-19

## 任务
`index.html` 手机版左上角菜单中 `Become a seller` 按钮点击无效，需要支持跳转到 `become-seller.html`。

## 修改页面
- `src/pages/index.html`
- `index.html`（构建产物）
- `codex_tasks/TASKS.md`

## 变更摘要
- 将移动端菜单中的 `Become a seller` 由纯文本节点改为链接节点：
  - `href="become-seller.html"`
  - `target="_self"`
- 增加 `text-decoration: none;`，确保链接显示与原文本样式一致，不引入默认下划线样式。
- 仅修改 `index.html` 对应菜单项跳转能力，未改动 PC 端结构与其他交互逻辑。
- 按规则执行 `node scripts/build-pages.js`，同步根目录构建产物并更新防缓存版本号。

## 影响范围
- 仅影响 `index.html` 手机版左上角菜单中 `Become a seller` 项的点击跳转行为。
- 不影响其他页面功能。
