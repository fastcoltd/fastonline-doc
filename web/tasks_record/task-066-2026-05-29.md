# Task 066 - 2026-05-29

## 任务
`demand-detail.html` 手机版中，顶部摘要卡片在页面滚动后吸顶时，样式不能变成铺满整宽；需要保持与未滚动时一致的卡片效果（含背景、圆角、间距）。

## 修改页面
- `css/detail/demand-detail.less`
- `css/detail/demand-detail.css`
- `demand-detail.html`
- `codex_tasks/TASKS.md`

## 变更摘要
- 仅修改 `demand-detail` 的手机版样式（`@media (max-width: 768px)`），PC 端未改动。
- 为 `.demand-summary-card.is-sticky` 增加移动端覆盖：保留卡片边框与圆角，并保持 `overflow: hidden`，避免吸顶后视觉变成整宽平铺。
- 关闭 `.demand-summary-card.is-sticky::before` 与 `::after`，移除吸顶态额外的全宽分割线/阴影效果，确保滚动前后视觉一致。
- 按构建规则执行 `node scripts/build-pages.js`，并仅保留 `demand-detail.html` 的资源版本号更新，确保手机端防缓存生效。
- 用户确认“任务完成”后，将 `codex_tasks/TASKS.md` 对应任务状态更新为“已完成”。

## 影响范围
- 仅影响 `demand-detail.html` 手机版顶部摘要卡片的吸顶展示效果及该页面的资源版本号。
- 不影响 PC 端及其他页面业务逻辑。
