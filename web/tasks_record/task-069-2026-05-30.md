# Task 069 - 2026-05-30

## 任务
`item-detail.html` 手机版中，购买信息区（`In stock / Price / Wholesale Price`）纵向间距不符合设计稿，需要按反馈逐步微调至目标效果。

## 修改页面
- `css/detail/item-detail.less`
- `css/detail/item-detail.css`
- `codex_tasks/TASKS.md`

## 变更摘要
- 仅修改 `item-detail` 的手机版样式（`@media (max-width: 768px)`），未改动 PC 端。
- 调整 `Price` 与 `Wholesale Price` 之间的垂直间距：
  - 在 `.item-detail-purchase-buy-section` 设置 `margin-top: -0.5rem;`，压缩两行间距。
- 调整 `In stock` 行顶部间距：
  - 在 `.item-detail-purchase-stock-row` 设置 `margin-top: 1.25rem;`（+10px）。
- 用户确认“任务完成”后，将 `codex_tasks/TASKS.md` 对应任务状态更新为“已完成”。

## 影响范围
- 仅影响 `item-detail.html` 手机版购买信息区纵向间距表现。
- 不影响 PC 端样式与其他页面功能逻辑。
