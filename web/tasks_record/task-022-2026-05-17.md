# Task 022 - 2026-05-17

## 任务
- `service.html` 手机版：修复面包屑区域布局（左侧间距、与下方内容间距），并将面包屑以上区域对齐 `item-all.html` 手机版。

## 修改内容
- 仅针对手机版进行调整，不影响 PC 布局。
- `service` 页面头部容器改为复用 `item-all` 同类结构：`page-head page-head-main`。
- 在移动端下补齐面包屑容器对齐规则：`width/margin-left/transform/padding/box-sizing`。
- 在移动端下调整面包屑与正文间距：`page-content` 顶部间距为 `0.5rem`。
- 更新 `service.js` 中头部 transform 判定，兼容 `page-head-main` 场景。
- 给 `service.css/js` 增加版本参数 `v=20260517-7`，避免移动端缓存旧资源。
- 执行 `node scripts/build-pages.js` 同步根目录产物。

## 变更文件
- `src/pages/service.html`
- `service.html`
- `css/detail/service.less`
- `css/detail/service.css`
- `js/detail/service.js`
- `codex_tasks/TASKS.md`

## 结果
- `service.html` 首个手机版任务状态已标记为已完成。
- 手机版目标区域已按任务要求完成调整。
