# Task 060 - 2026-05-23

## 任务
`search-all.html` 手机版中，`Campaigns` tab 的 item 改为复用 `compaign-all.html` 的 `Horizontal` 模式组件，并修复复用后收藏按钮位置错位问题（重点兼容一加 Ace5 Pro）。

## 修改页面
- `src/pages/search-all.html`
- `src/partials/components/search-all-campaign-item.html`
- `css/search-all.less`
- `css/search-all.css`
- 根目录 `search-all.html` 产物（`node scripts/build-pages.js` 生成）
- `codex_tasks/TASKS.md`

## 变更摘要
- 仅修改手机版展示逻辑，PC 保持原有 `Campaigns` 卡片结构。
- 在 `search-all` 的 `Campaigns` 卡片中：
  - 保留原有结构用于 PC（`search-card-second-item-desktop`）；
  - 新增移动端复用结构（`search-card-second-item-mobile`），直接复用 `compaign-all` 横向组件。
- 为保证复用组件样式完整，在 `search-all` 页面引入 `compaign.css`。
- 同步样式到 `less/css`，并做移动端兼容修复：
  - 新增移动端桌面/手机结构显示切换规则；
  - 为 `search-card-second-item-mobile` 下的复用卡片强制设置相对定位；
  - 为收藏按钮设置绝对定位（`right: 1.25rem; top: 0.75rem; z-index: 2;`），修复按钮错位。
- 执行 `node scripts/build-pages.js`，更新根目录页面与防缓存版本号（`v=20260523102353`）。
- 将 `TASKS.md` 对应任务状态更新为“已完成”。

## 影响范围
- 仅影响 `search-all.html` 手机版 `Campaigns` tab 的 item 展示与收藏按钮定位。
- 不影响 PC 端及其他页面功能。
