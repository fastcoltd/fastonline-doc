# Task 102：补全faq-detail.html页面，包括 pc 版和手机版

## 任务内容

补全 `faq-detail.html` 页面，header、top-menu、footer 与 `index.html` 保持一致，页面内容参考设计稿实现，并复用项目中已有的收藏、点赞/点踩、分享组件。

## 修改页面

- `faq-detail.html`

## Diff 总结

- 调整 `src/pages/faq-detail.html` 的详情头部结构：
  - 标题和收藏按钮放在同一行。
  - 收藏数量、分类、品牌、作者、阅读数、发布时间等信息移动到下一行。
  - 为 `css/faq.css` 添加防缓存参数。
- 调整 `css/faq.less` 和 `css/faq.css`：
  - 覆盖全局 `header` flex 行布局对 FAQ 详情头部的影响。
  - 让 `.faq-detail-header` 使用纵向布局。
  - 让标题行和信息行都占满内容宽度，pc 和手机版均生效。
- 通过 `node scripts/build-pages.js` 同步生成根目录产物 `faq-detail.html`。

## 验证

- 已执行 `node scripts/build-pages.js`。
- 已执行 `git diff --check`。
- 已通过 Chrome 调试分别验证桌面和手机布局：
  - 标题和收藏按钮同一行。
  - 其他信息在下一行。
  - 手机信息行从内容左侧开始展示。
