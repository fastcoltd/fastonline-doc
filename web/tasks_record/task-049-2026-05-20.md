# Task 049 - 2026-05-20

## 任务
`tag-all.html` 手机版 Posts 区块修复：将 post 列表替换为 `post-all.html` 第二个 item 同款组件，并确保手机端样式与缓存刷新在一加 Ace5 Pro 上可生效。

## 修改页面
- `src/pages/tag-all.html`
- `css/tag-all.less`
- `css/tag-all.css`
- `tag-all.html`（由 `node scripts/build-pages.js` 生成）
- `codex_tasks/TASKS.md`

## 变更摘要
- 仅针对手机版修改，不影响 PC 端。
- `tag-all` 的 `#post-item` 列表由原 4 个 `index-host-post-*` 变体替换为 `post-shared-item`（参数与 `post-all.html` 第二个 item 对齐）。
- 为保证手机端呈现一致，补充 `#post-item #hot-posts .post-item` 的移动端关键覆盖：
  - 收藏按钮右上定位（`top/right/left`）；
  - 标题区域右侧留白（`padding-right`）。
- 同步修改 `tag-all.less` 与 `tag-all.css`，保持源码与编译产物一致。
- 执行 `node scripts/build-pages.js`，刷新 `tag-all.html` 资源版本号（`v=20260520131944`），确保手机端防缓存生效。
- 已将 `TASKS.md` 对应任务状态更新为“已完成”。

## 影响范围
- 仅影响 `tag-all.html` 页面手机版 Posts 列表样式与结构。
- 其他页面与 PC 端功能不受影响。
