# Task 044 - 2026-05-20

## 任务
`brand-all.html` 手机版顶部布局修复：完全复用 `item-all.html` 的顶部头部组件结构，保证顶部展示一致。

## 修改页面
- `src/pages/brand-all.html`
- `brand-all.html`（由 `node scripts/build-pages.js` 生成）
- `codex_tasks/TASKS.md`

## 变更摘要
- 仅做最小化改动，聚焦手机版顶部区域，不影响其他功能。
- 将 `brand-all` 页面顶部头部容器类从
  - `page-head page-top-sticky page-top-sticky-main`
  调整为
  - `page-head page-head-main`
  以直接复用 `item-all.html` 的头部结构模式。
- 按源码构建规则执行 `node scripts/build-pages.js`，同步生成根目录 `brand-all.html`，并刷新防缓存版本号（`20260520061603`）。
- 已将 `TASKS.md` 中该任务状态更新为“已完成”。

## 影响范围
- 仅影响 `brand-all.html` 顶部头部容器结构表现。
- 其余页面与 PC 端其他逻辑不变。
