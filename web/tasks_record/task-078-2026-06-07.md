# Task 078 - store-detail.html Overview 图表切换

## 任务内容

- 页面：`store-detail.html`
- 范围：手机版和 PC 版
- 要求：Overview 区域的 `7D / 14D / 30D` 按钮点击后，下面的曲线图需要发生数据变化，而不只是样式变化。
- 补充：检查项目中同样样式的地方，按钮点击后都需要发生数据变化。

## 修改摘要

- 在 `src/pages/store-detail.html` 中为 Overview 图表补充 `7D / 14D / 30D` 三组不同数据。
- 修改 Overview 图表渲染函数，支持按周期参数重绘数据，并复用已有 ECharts 实例，避免重复初始化。
- 在 `js/detail/store-detail.js` 中增强 Overview tab 交互：
  - 根据按钮文本自动识别周期。
  - 点击和键盘触发都会刷新图表数据。
  - 保留原有 active 状态与 `aria-pressed` 状态切换。
- 调整 Overview 图表底部布局，避免 `25d` 等 x 轴标签与 `Goods rate / Response rate / Sufficient rate` 图例重合：
  - 日期标签位于上一行。
  - 图例位于下一行并横向单行展示。
- 检查同样样式的 `item-detail.html`，已有 `7D / 14D / 30D` 数据切换逻辑，本次未改动。
- 执行 `node scripts/build-pages.js` 重新生成根目录页面，并刷新本地 CSS/JS 防缓存版本号。
- 将 `codex_tasks/TASKS.md` 中对应任务状态标记为已完成。

## 语法和构建检查

- `node scripts/build-pages.js`：通过。
- `node --check js/detail/store-detail.js`：通过。
- `src/pages/store-detail.html` 和 `store-detail.html` 内联脚本解析检查：通过。

## 主要文件

- `src/pages/store-detail.html`
- `store-detail.html`
- `js/detail/store-detail.js`
- `codex_tasks/TASKS.md`

