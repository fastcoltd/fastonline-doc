# Task 101：新增system-post-detail.html页面，需要支持pc版和手机版

## 任务内容

绝大多数内容和 `post-detail.html` 是一样的。pc 端区别：顶部信息改为 system post 样式，锚点组件在左侧。手机端区别：头部按 system post 样式展示。要求复制一份 `post-detail.html` 的代码出来，避免影响 `post-detail.html`。

## 修改页面

- `system-post-detail.html`

## Diff 总结

- 调整 `src/pages/system-post-detail.html` 顶部 meta 文案符号，并为独立 css/js 增加防缓存参数。
- 调整 `css/detail/system-post-detail.less` 和 `css/detail/system-post-detail.css` 中 system post 顶部布局：
  - pc 端对齐参考图中的 `Author / Reads / Last Edited on` 信息行。
  - 手机端改为标题、标签+操作按钮、meta 信息三段式布局，匹配参考图 `image-151.png`。
- 通过 `node scripts/build-pages.js` 同步生成根目录产物 `system-post-detail.html`。
- 将 `codex_tasks/TASKS.md` 中该任务状态标记为 `已完成`。

## 验证

- 已执行 `node scripts/build-pages.js`。
- 已执行 `node --check js/detail/system-post-detail.js`。
- 已执行 `git diff --check`。
