# Task 071 - 2026-06-01

## 任务
`search-all.html` 手机版中，不同类型 item 的标签样式不一致；复用项目已有标签组件样式，统一应用到该页面所有类型 item。

## 修改页面
- `css/search-all.less`
- `css/search-all.css`
- `search-all.html`
- `codex_tasks/TASKS.md`

## 变更摘要
- 仅修改 `search-all` 的**手机版样式**（`@media (max-width: 768px)`），未改动 PC 样式。
- 在 `search-all.less` 新增统一覆盖规则，将以下移动端卡片中的标签统一为同一套 `item-tag / item-tag-text` 样式：
  - `search-card-second-item-mobile`（campaign）
  - `search-card-third-item-mobile`（post）
  - `search-card-fourth-item-mobile`（store）
  - `search-card-fifth-item-mobile`（demand）
- 统一后的标签样式：
  - 标签容器：`height: 2.25rem`、`min-height: 2.25rem`、`padding: 0 0.5rem`、`border-radius: 1.125rem`
  - 标签文本：`font-size: 1.25rem`、`line-height: 1.75rem`、`color: var(--gray-color)`
  - 统一去除额外阴影：`box-shadow: none`
- 同步更新编译产物 `css/search-all.css`。
- 执行 `node scripts/build-pages.js` 以更新缓存版本，`search-all.html` 资源 `v` 参数更新为 `20260531162151`，确保手机端可拉取最新样式。
- 用户确认“任务完成”后，将 `codex_tasks/TASKS.md` 对应任务状态更新为“已完成”。

## 影响范围
- 仅影响 `search-all.html` 手机版各类 item 的标签视觉一致性。
- 不影响页面业务逻辑，不影响 PC 端展示。
