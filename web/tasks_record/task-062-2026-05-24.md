# Task 062 - 2026-05-24

## 任务
`search-all.html` 手机版中，`Stores` tab 的 item 改为复用 `store-all.html` 的 Horizontal 模式 item 组件，要求与参考页展示一致（重点兼容一加 Ace5 Pro）。

## 修改页面
- `src/partials/components/search-all-store-item.html`
- `css/search-all.less`
- `css/search-all.css`
- 根目录 `search-all.html` 产物（`node scripts/build-pages.js` 生成）
- `codex_tasks/TASKS.md`

## 变更摘要
- 仅修改手机版展示逻辑，PC 保持原有 `Stores` 卡片结构。
- 在 `search-all` 的 `Stores` 卡片中：
  - 保留原有结构用于 PC（`search-card-fourth-item-desktop`）；
  - 新增移动端复用结构（`search-card-fourth-item-mobile`），直接复用 `store-all-horizontal-mobile-item` 组件。
- 同步样式到 `less/css`：
  - 新增 `search-card-fourth-item-mobile` 默认隐藏规则；
  - 在 `max-width: 768px` 下隐藏桌面结构、显示移动结构；
  - 补齐复用组件在 `search-all` 页下的容器与子元素样式，确保与 `store-all.html` Horizontal 手机版一致。
- 执行 `node scripts/build-pages.js`，更新根目录页面与防缓存版本号（`v=20260523155352`）。
- 将 `TASKS.md` 对应任务状态更新为“已完成”。

## 影响范围
- 仅影响 `search-all.html` 手机版 `Stores` tab 的 item 展示。
- 不影响 PC 端及其他页面功能。
