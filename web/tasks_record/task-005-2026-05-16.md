# Task 005 完成记录

- 任务：`codex_tasks/TASKS.md` - Task（`post-all.html` 排序按钮太小）
- 完成日期：2026-05-16
- 修改范围：仅 `post-all` 页面手机版排序按钮尺寸，不影响 PC 端与其他页面

## 本次 Diff 总结

1. 按任务要求通过 Figma MCP 读取设计稿节点：
   - 链接节点：`node-id=614:7799`
   - 关键尺寸：排序图标为 `24px × 24px`。

2. 调整 `post-all` 手机版排序按钮图标尺寸：
   - 将 `.post-all-filter-layoutcontrol-box .page-sort-icon` 从 `2rem` 调整为 `3rem`（对应 24px）；
   - 增加 `min-width: 3rem` 与 `min-height: 3rem`，避免在移动端布局中被压缩变小。

3. 同步维护源码与产物：
   - 同步修改 `css/post-all.less` 与 `css/post-all.css`，保持一致。

## 涉及文件

- `css/post-all.less`
- `css/post-all.css`
- `codex_tasks/TASKS.md`
