# Task 045 - 2026-05-20

## 任务
`compaign-detail.html` 手机版顶部布局修复：完全复用 `item-all.html` 的顶部组件结构，保证顶部展示一致。

## 修改页面
- `src/pages/compaign-detail.html`
- `css/detail/compaign-detail.less`
- `css/detail/compaign-detail.css`
- `compaign-detail.html`（由 `node scripts/build-pages.js` 生成）
- `codex_tasks/TASKS.md`

## 变更摘要
- 仅做最小化改动，聚焦手机版顶部区域，不影响 PC 端其它功能。
- 将 `compaign-detail` 页面顶部头部容器类从
  - `page-top-sticky page-top-sticky-main`
  调整为
  - `page-head page-head-main`
  以复用 `item-all.html` 的头部结构模式。
- 在移动端媒体查询（`max-width: 768px`）补齐面包屑容器对齐规则：
  - `width: 100%`
  - `margin-left: 0`
  - `transform: none`
  - `padding: 0`
  与 `item-all.html` 手机版同位置布局保持一致。
- 已同步维护 `less/css` 两份样式文件一致。
- 按源码构建规则执行 `node scripts/build-pages.js`，同步生成根目录 `compaign-detail.html`，并刷新防缓存版本号（`20260520080729`）。
- 已将 `TASKS.md` 中该任务状态更新为“已完成”。

## 影响范围
- 仅影响 `compaign-detail.html` 页面手机版顶部与面包屑布局表现。
- 其它页面与 PC 端不受影响。
