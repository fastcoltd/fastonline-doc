# Task 065 - 2026-05-29

## 任务
`post-detail.html` 手机版中，`Hot` 与 `Limited time sale` 标签需要紧跟标题显示，不论标题是一行还是两行，都不能另起一行。

## 修改页面
- `css/detail/post-detail.less`
- `css/detail/post-detail.css`
- `codex_tasks/TASKS.md`

## 变更摘要
- 仅修改 `post-detail` 的手机版样式（`@media (max-width: 768px)`），PC 端样式未改动。
- 将标题容器从移动端纵向布局改为行内流布局，避免标签被强制换行到下一行。
- 调整标题样式为 `inline` 且取消移动端高度裁切，保证标签可以在标题文本后自然跟随。
- 调整标签容器与标签本身为行内展示（`inline` / `inline-flex`），并设置首个标签与后续标签间距，使 `Hot` 和 `Limited time sale` 紧贴标题后展示。
- 同步更新 `less` 与 `css`，保持源码与编译产物一致。
- 按确认结果，将 `codex_tasks/TASKS.md` 的首个任务状态更新为“已完成”。

## 影响范围
- 仅影响 `post-detail.html` 手机版顶部标题与标签区域的排版。
- 不影响 PC 端及其他页面。
