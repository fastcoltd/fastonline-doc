# Task 015 - 2026-05-17

## 任务信息
- 任务：`post-detail.html` 手机版锚点浮动按钮与右侧锚点抽屉实现及对齐
- 目标：按 Figma（`852:28733`、`997:36993`）实现移动端锚点入口与抽屉，并完成交互闭环

## 修改文件
- `src/pages/post-detail.html`
- `src/partials/components/detail-page-menu.html`（新增）
- `image/detailpage/post-detail-menu-mobile.png`（新增）
- `js/detail/post-detail.js`
- `css/detail/post-detail.less`
- `css/detail/post-detail.css`
- `post-detail.html`（由构建脚本生成）
- `codex_tasks/TASKS.md`

## Diff 摘要
1. 新增移动端锚点入口组件
- 新增可复用 partial：`detail-page-menu.html`。
- 在 `post-detail` 页面引入该组件，并与锚点抽屉建立 `aria-controls` 关联。

2. 移动端锚点抽屉样式按设计稿对齐
- 增加遮罩层与右侧抽屉卡片样式（圆角、边框、层级、尺寸、间距）。
- 抽屉打开时隐藏入口按钮，仅保留底部关闭按钮。
- 修复按钮显示细节：去除按钮默认边框/背景，按钮图按整数像素 `56px` 渲染，消除底部灰线伪影。

3. 锚点交互增强
- 修复移动端菜单开关逻辑，统一为 `is-open` 状态控制。
- 新增点击锚点标题后的行为：
  - 跳转到对应 section。
  - 自动收起锚点抽屉。

4. 构建与验证
- 执行 `node scripts/build-pages.js` 同步根目录产物。
- 执行 `node --check js/detail/post-detail.js` 语法检查。
- 使用 chrome MCP 验证：
  - 按钮样式与显示。
  - 抽屉打开/关闭。
  - 点击标题后跳转并自动收起。

5. 任务状态维护
- 将 `codex_tasks/TASKS.md` 对应任务状态更新为“已完成”。

## 结果
- `post-detail` 手机版已具备可用且可关闭的锚点抽屉，入口按钮与交互均对齐任务要求；未改动 PC 版本逻辑。
