# Task 103：faq-detail.html 手机版第二行自动换行补充修改

## 任务内容

`faq-detail.html` 手机版 top menu 下方的第二行信息需要自动换行，仅修改手机版。

## 修改页面

- `faq-detail.html`

## Diff 总结

- 调整 `css/faq.less` 和 `css/faq.css` 的手机版规则：
  - `.faq-detail-info-row` 改为 `flex-wrap: wrap`。
  - 取消手机端横向滚动和强制不换行。
- 通过 `node scripts/build-pages.js` 同步生成根目录产物 `faq-detail.html`。

## 验证

- 已执行 `node scripts/build-pages.js`。
- 已执行 `git diff --check`。
- 已通过 Chrome 调试确认手机版第二行信息 `flex-wrap` 生效，并实际换成多行展示。
