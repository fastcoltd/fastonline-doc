# Task 110：blog.html 页面的手机版头部对齐

## 任务内容

blog.html 页面的手机版头部部分，左右两侧需要和红线对齐：左侧和面包屑左侧对齐，右侧距离屏幕边缘的距离与左侧相同。

## 修改页面

- `blog.html`

## Diff 总结

- 在 `css/detail/blog.less` 的手机端样式中，将 `.blog-intro-section` 左右内边距从 `1.25rem` 调整为 `0`，使头部区域与面包屑左侧对齐。
- 同步更新编译产物 `css/detail/blog.css`。
- 更新 `src/pages/blog.html` 中 `blog.css` 的版本参数，用于防缓存。
- 执行 `node scripts/build-pages.js` 重新生成根目录 `blog.html`，同步最新缓存版本。

## 检查

- `git diff --check` 通过。
- 未调用 chrome mcp，按任务规则由用户自行确认效果。
