# Task 004 完成记录

- 任务：`codex_tasks/TASKS.md` - Task（`brand.html` 手机版 Hot Posts item 改造）
- 完成日期：2026-05-16
- 修改范围：仅 `brand.html` 手机版 Hot Posts 区域，未影响 PC 端与其他页面

## 本次 Diff 总结

1. `brand` 页 Hot Posts 改为响应式双结构：
   - 保留桌面端原有 `index-host-post-item-*` 结构；
   - 新增手机版专用结构，直接复用 `post-all` 同款 `post-shared-item` 组件。

2. 新增品牌页移动端切换规则与对齐规则：
   - 手机端隐藏桌面结构、显示手机版 `post-shared-item`；
   - 同步 `post-all` 手机版关键表现（收藏按钮位置、标题右侧留白）。

3. 修复卡片固定高度导致内容溢出：
   - 将手机版 Hot Posts 卡片由固定高度改为 `height: auto`，并保留 `min-height`；
   - 限定该规则只作用于 `brand-hot-post-item-mobile`，不影响其他模块。

4. 按反馈调整 Kind 区块展示策略：
   - Kind 区块最多展示两行（`max-height: 5rem`）；
   - 超出内容在 card 范围外直接裁切（`overflow: hidden`）；
   - 保持 tag 文本单行展示，避免撑出卡片。

5. 源码与产物同步：
   - 修改 `src` 源码后执行 `node scripts/build-pages.js`；
   - 生成并同步根目录 `brand.html` 产物。

## 涉及文件

- `src/partials/components/brand-hot-posts-section.html`
- `css/detail/brand.less`
- `css/detail/brand.css`
- `brand.html`
- `codex_tasks/TASKS.md`
