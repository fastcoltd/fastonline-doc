# Task 126：多个页面的手机版悬浮按钮右边距调整

## 任务内容

多个页面的手机版本中，有悬浮按钮，举个例子，post-detail.html页面中的悬浮按钮如图 image-172.png，悬浮按钮需要增加右边距。悬浮按钮的右边距离屏幕边缘的宽度，请调整成顶部面包屑左侧距离屏幕边缘的宽度相同。其他的有悬浮按钮的页面，也需要进行相同的修改，为悬浮按钮增加右边距。所有手机版的悬浮按钮样式是一模一样的，按照这个来查找即可，再次强调所有页面的悬浮按钮都需要修改。

## 修改页面

- post-detail.html
- item-detail.html
- blog.html
- service.html
- resource-detail.html
- system-post-detail.html
- fastesp-service.html
- 其他构建产物页面同步更新缓存版本号

## Diff 总结

- 在 `css/page.less` 的移动端 `.detail-page-menu` 公共样式中增加 `right: 2rem;`。
- 同步更新编译产物 `css/page.css`。
- 通过 `node scripts/build-pages.js` 重新生成根目录 HTML 产物，缓存版本更新为 `20260614084059`，确保手机端查看最新效果。

## 检查

- 已执行 `node scripts/build-pages.js`
- 已执行 `git diff --check`
- 未调用 chrome mcp
