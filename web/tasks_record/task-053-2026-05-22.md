# Task 053 - 2026-05-22

## 任务
`compaign-detail.html` 手机版：`Related Items` 区块需要使用与当前页面 `items` 相同的 item 组件。

## 修改页面
- `src/pages/compaign-detail.html`
- `css/detail/compaign-detail.less`
- `css/detail/compaign-detail.css`
- `codex_tasks/TASKS.md`
- 根目录 `compaign-detail.html` 产物（`node scripts/build-pages.js` 生成，含缓存版本更新）

## 变更摘要
- 仅针对手机版生效，不影响 PC。
- 为 `Related Items` 增加移动端横排容器，并在移动端隐藏原竖排容器、显示横排容器。
- `Related Items` 横排组件最终切换为与本页 `items` 相同的 `item-all-horizontal-item`（不再使用 `compaign-all-horizontal-item`）。
- 修复移动端显隐优先级，确保覆盖全局 `data-layout` 显隐规则，避免一加 Ace5 Pro 上仍显示旧结构。
- 执行 `node scripts/build-pages.js` 刷新产物与防缓存版本。
- 将 `TASKS.md` 对应任务状态更新为“已完成”。

## 影响范围
- 影响 `compaign-detail.html` 手机版 `Related Items` 区块展示组件与布局。
- 不影响 PC 布局和其他页面业务逻辑。
