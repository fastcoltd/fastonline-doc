# Task 027 - 2026-05-18

## 任务信息
- 任务：`demand-detail.html` 手机版 Bidding information 列表项布局与评分组件调整
- 目标：将列表中所有 item 的评分改为复用现有单图标评分组件，并按 Figma 节点 `1152:44658` 完成移动端布局调整；仅改手机版，不影响 PC。

## 修改文件
- `src/partials/components/demand-detail-bidding-item.html`
- `css/detail/demand-detail-item.less`
- `css/detail/demand-detail-item.css`
- `demand-detail.html`
- `codex_tasks/TASKS.md`

## Diff 摘要
1. 评分组件复用改造
- 在 `demand-detail-bidding-item.html` 中移除列表项原有手写评分结构。
- 改为 include 复用 `rating-with-count` 组件（移动端显示为单图标评分样式），满足“使用现成组件”的要求。

2. 列表项结构最小化增强
- 为目标卡片增加作用域类：`demand-detail-bidding-card`、`demand-detail-bidding-content`。
- 为统计项增加细分作用域类：`demand-detail-item-stat`、`demand-detail-item-stat-time`。
- 保持组件复用和最小改动，避免影响其它模块。

3. 移动端布局按 Figma 对齐（仅手机版）
- 在 `demand-detail-item.less/css` 的 `@media (max-width: 768px)` 下，针对 `demand-detail-bidding-card` 作用域调整：
  - 卡片圆角/内边距、角标尺寸位置、右上操作按钮尺寸位置。
  - 头像区、标题区、标记区、评分区间距与换行行为。
  - 统计区换行、文字显隐与时间图标样式。
  - 标签区 chip 尺寸与字号、图片区间距与尺寸、文案字号与行高。
- 所有调整均限定在移动端作用域，PC 样式不变。

4. 页面产物同步
- 按构建规则执行 `node scripts/build-pages.js`，同步生成根目录 `demand-detail.html`。

5. 任务状态更新
- 将 `codex_tasks/TASKS.md` 中该任务状态由“未完成”更新为“已完成”。

## 校验
- 已执行 `node scripts/build-pages.js`，页面成功生成。
- 变更范围仅包含本任务目标模块与任务记录文件。
- 样式变更限定在移动端媒体查询及组件作用域内，不影响 PC。

## 结果
- `demand-detail.html` 手机版 Bidding information 列表项已完成评分组件复用与布局对齐修改。
- 任务状态与任务记录已同步更新，可进入下一任务。
