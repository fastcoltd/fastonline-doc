# Task 040 - 2026-05-20

## 任务
`resource-detail.html` 手机版修复：点击收藏按钮时去掉整块区域的蓝色点按高亮效果，同时保持收藏/取消收藏功能不变。

## 修改页面
- `css/detail/resource-detail.less`
- `css/detail/resource-detail.css`
- `resource-detail.html`（由 `node scripts/build-pages.js` 生成）
- `codex_tasks/TASKS.md`

## 变更摘要
- 仅针对移动端（`max-width: 768px`）增强收藏按钮点击态兼容处理：
  - 为 `.resource-detail-like-button` 增加 `-webkit-tap-highlight-color: transparent`；
  - 增加 `-webkit-touch-callout: none`、`touch-action: manipulation`、`user-select: none`；
  - 补充 `.resource-detail-like-button:focus/:focus-visible/:active` 的无高亮样式（`outline: none; background: transparent;`）。
- 对标题行相关容器补充移动端 tap 高亮抑制，避免安卓机型点击时出现整块蓝色反馈。
- 按源码构建规则执行 `node scripts/build-pages.js`，同步生成根目录 `resource-detail.html`，刷新防缓存版本号（`20260520004715`）。
- 已将 `TASKS.md` 中本任务状态更新为“已完成”。

## 影响范围
- 仅影响 `resource-detail.html` 手机版收藏按钮点击视觉反馈。
- 收藏/取消收藏交互逻辑不变，PC 端不受影响。
