# Task 041 - 2026-05-20

## 任务
`attribute-all.html` 手机版修改：`demands` 的 `Vertical display` 模式下 item 布局不正确，改为直接复用 `demand-all.html` 的 Vertical item，要求展示效果一致。

## 修改页面
- `src/pages/attribute-all.html`
- `attribute-all.html`（由 `node scripts/build-pages.js` 生成）
- `codex_tasks/TASKS.md`

## 变更摘要
- 仅针对手机版需求改动 `attribute-all` 的 Demand 列表结构，未改动 PC 功能逻辑。
- 将 `#demand-item` 下 `data-layout="vertical"` 容器从旧的 `index-popular-demand-item-*` include 切换为：
  - 容器类名复用 `demand-all-figma-container`；
  - 内容复用 `../partials/components/demand-all-figma-items.html`。
- 这样 Vertical 模式与 `demand-all.html` 使用同一套组件链路（含移动端 vertical/horizontal 子项），保证样式与布局一致。
- 按源码构建规则执行 `node scripts/build-pages.js`，同步生成根目录 `attribute-all.html`，并刷新该页面防缓存版本号（`20260520015528`）。
- 已将 `TASKS.md` 中该任务状态更新为“已完成”。

## 影响范围
- 仅影响 `attribute-all.html` 页面中 Demand 分类的 Vertical display 展示。
- 其他页面与 PC 端不受影响。
