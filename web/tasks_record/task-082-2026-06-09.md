# Task 082：post-detail.html 手机版正文左右边距对齐

## 任务内容

`post-detail.html` 手机版正文内容的左右边距需要和顶部标题部分一致，即正文左侧和右侧分别与截图红线对齐。

## 修改页面

- `post-detail.html`

## 修改内容

- 调整 `css/detail/post-detail.less` 中移动端 `.article-content` 的左右 padding，从原来的 `0 2rem` 改为左右 `0`，避免在已有 `body` 移动端左右边距基础上再次缩进。
- 同步更新 `css/detail/post-detail.css`。
- 移动端 `.section-content` 和内部 `p` 清除 `margin`、`padding`、`text-indent`，避免段落缩进影响对齐。
- 通过 `node scripts/build-pages.js` 重新生成根目录 `post-detail.html`，刷新资源版本防缓存。

## 验证

- 使用真实 Chrome Headless 的 DevTools Protocol 按一加 Ace5 Pro 等效视口 `393x873`、DPR `2.75`、Android UA 测量。
- 验证结果：顶部标题区域、正文标题、正文段落左侧均为 `16px`，右侧均为 `377px`。
- 已确认生成页资源版本刷新为 `20260608172514`，并保留 no-cache meta。
