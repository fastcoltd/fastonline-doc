# Task 068 - 2026-05-30

## 任务
`item-detail.html` 手机版中，“发货”信息位置不正确；要求按设计将该部分靠右显示。

## 修改页面
- `css/detail/item-detail.less`
- `css/detail/item-detail.css`
- `codex_tasks/TASKS.md`

## 变更摘要
- 仅修改 `item-detail` 的手机版样式（`@media (max-width: 768px)`），PC 端未改动。
- 在 `.item-detail-purchase-stock-row` 中将布局调整为两端对齐，并禁止换行：
  - `justify-content: space-between;`
  - `flex-wrap: nowrap;`
- 使“发货”信息块在同一行靠右显示，与“库存”信息分列两端。
- 用户确认“任务完成”后，将 `codex_tasks/TASKS.md` 对应任务状态更新为“已完成”。

## 影响范围
- 仅影响 `item-detail.html` 手机版购买区“库存/发货”这一行的对齐方式。
- 不影响 PC 端样式与其他页面业务逻辑。
