# Task 129: brand-all.html 手机版 - 字母锚点左侧与面包屑对齐

**日期:** 2026-06-17

**任务内容:**
brand-all.html 手机版，左侧的字母锚点部分，左侧需要和面包屑的左侧对齐。

**修改文件:**
- `css/brand.css` — 手机端 `.brand-page-index-box` 的 `padding` 从 `0 1rem 1rem` 改为 `0 1rem 1rem 0.5rem`，新增 `align-items: flex-start`
- `css/brand.less` — 同步修改

**修改详情:**

| 属性 | 修改前 | 修改后 | 说明 |
|------|--------|--------|------|
| padding-left | 1rem | 0.5rem | 减少左内边距，字母向左移动 |
| align-items | center（继承） | flex-start | 字母从居中对齐改为左对齐，使字母左侧与面包屑左侧对齐 |

**影响范围:** 仅 brand-all.html 手机版（`@media screen and (max-width: 768px)`），不影响 PC 版和其他页面。

**缓存处理:** 已通过 `node scripts/build-pages.js` 重新构建所有页面，缓存版本号更新为 `20260617010625`。
